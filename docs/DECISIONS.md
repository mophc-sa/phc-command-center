# DECISIONS — سجل قرارات PHC Command Center

> الأحدث في الأعلى. لا تحذف قرارًا — إن تغيّر، أضِف قرارًا جديدًا يشير إليه.
> القرارات أدناه مستخلَصة من الحالة القائمة للمستودع (يوليو 2026) — راجعها وصحّح أي تفصيل.

---

## 2026-08-18 — D19: كل خريطة انتقالات مزدوجة تُحرَس باختبار يقارن النسختين
**القرار:** أي خريطة حالات موجودة في العميل والخادم معاً يجب أن يقارنها اختبار عقد. ينطبق الآن على المبيعات (`sales-transitions.contract.test.ts`) والمناقصات (`tender-transitions.contract.test.ts`).

**السبب:** خريطة المبيعات انحرفت بصمت. العميل يعرض `jih_bafo` و`contract_signed`، والخادم يرفضهما بـ409 — فمرحلتان من دورة الحياة الكنسية في الـPRD **غير قابلتين للوصول عملياً**. والأسوأ أن هذا **تكرار حرفي** لعطل سبق إصلاحه في خرائط المناقصات، وتعليق في الكود يصفه بدقة — لكن الدرس لم يُعمَّم على الخريطة المجاورة. اختبار واحد كان سيمنع تكراره.

**كيف يُستخدم:** عند إضافة أي حالة لأي خريطة، أضِفها في الموضعين، ودع الاختبار يفشل أولاً إن نسيت.

---

## 2026-08-18 — D20: تقدير الذكاء وتقدير الإنسان حقيقتان منفصلتان
**القرار:** `score` للذكاء و`human_win_probability` للإنسان، في أعمدة مستقلة. تعيين أحدهما لا يمسّ الآخر.

**السبب:** كان تقدير المدير يُكتب عبر `score_manual_override` **فوق** درجة النموذج فيمحوها. النتيجة أن السؤال الوحيد المفيد — أين يختلف النموذج عن المكتب، ومن أصاب — صار بلا إجابة بأثر رجعي. الاحتفاظ بالقيمتين يكلّف عمودين ويشتري قدرة على القياس.

---

## 2026-08-18 — D17: الاستقبال يقف عند المراجعة؛ التحويل ينتقل من الحفظ إلى الاعتماد
**القرار:** `createInboxItemAndRoute` لم تعد تحوّل. الطلب يُحفظ عند `pending_review`، و**نفس** دوال التحويل (`convertInboxToRfq` / `convertInboxToTender`) تعمل عند «اعتماد للتسعير» عبر `approveIntakeForPricing`.

**السبب:** [[D10]] و[[D11]] جعلا النموذج يوجّه نفسه لحظة الحفظ، وكان ذلك صحيحًا لمشكلته: أربع شاشات لإدخال طلب. لكنه عنى أن **لا شيء يُراجَع قبل دخول خط المبيعات** — وPRD 2026-08-12 §15 يشترط مراجعة الفرصة قبل التسعير. لم يُعَد بناء التحويل ولم تتغيّر تجربة المُدخِل؛ تغيّر **متى** يعمل فقط.

**كيف يُستخدم:** أي مسار جديد يُدخل سجلًا للأنبوب يمرّ بـ`approveIntakeForPricing`، لا باستدعاء التحويل مباشرة.

---

## 2026-08-18 — D18: نقص البيانات واقتراح النوع بقواعد حتمية لا بنموذج
**القرار:** فحص اكتمال حزمة الطلب واقتراح نوعه يُنفَّذان كدوال صرفة في `src/lib/intake-assessment.ts`، لا عبر وكيل ذكاء.

**السبب:** «هل وصل جدول الكميات؟» و«هل يوجد موعد نهائي؟» حقائق مكتوبة على السجل. تمريرها على نموذج يجعل الإجابة أبطأ وغير حتمية وغير قابلة للاختبار الوحدوي، مقابل صفر فائدة. الذكاء يبقى لما يحتاجه فعلًا: قراءة المرفقات غير المهيكلة واستخراج الحقول منها.

**كيف يُستخدم:** لا تُضِف وكيلًا لسؤال يمكن لاستعلام أن يجيبه. الوكيل لِما لا شكل له.

---

## 2026-08-12 — D16: «المشاريع المرساة» و«طابور الترسية» عرضٌ لا كيان
**القرار:** Awarded Projects بند تنقّل يشير إلى `‎/opportunities?stage=won` — أي عرض مفلتر على الكيان الكنسي. `‎/award-queue` و`‎/tender-conversion` و`‎/follow-ups` و`‎/targets` تبقى مساراتٍ عاملة لكنها تخرج من التنقّل.

**السبب:** مبدأ الـPRD «VIEW ≠ MODULE» و«ACTION ≠ PAGE». كل واحدة منها كانت تعرض مجموعة فرعية من الفرص كأنها كيان مستقل، فينقسم مفهوم «الفرصة» على أربع صفحات. الفلتر يعطي نفس القيمة بلا انقسام. **لم تُحذف المسارات** لأن الروابط المحفوظة والاختبارات تعتمدها، ولأن الحذف يخالف قاعدة «لا تحذف وظيفة تعمل».

**كيف يُستخدم:** أي «طابور» جديد يُبنى فلترًا على الكيان الأصلي أولًا. لا تُنشأ صفحة إلا إذا كانت طابورًا عابرًا للأقسام.

---

## 2026-08-12 — D15: `system_admin` إدارة تقنية فقط — بلا أي سلطة تجارية
**القرار:** `system_admin` لا يمنح وحده أيًّا من: خطوات BAFO الأربع، تعديل القيمة الإجمالية، أو أي اعتماد تجاري. مفروض في `roles.ts` وفي القاعدة (`20260812100000`). من يحتاج الصلاحية يحمل الدور التجاري المناسب، والأدوار تراكمية فتأتي منه وحده.

**السبب:** كان مكتوبًا في كل بوابة كـ«platform-admin override» (`20260727220000`). النتيجة أن حسابًا يحمل `system_admin` وحده يستطيع رفع طلب خصم واعتماد الخطوات الأربع عليه بنفسه — فالسلسلة تفرض **ترتيبًا لا أربعة أحكام مستقلة**. وهذا ليس نظريًا: مراجعة 2026-08-06 وجدت حسابًا حيًّا بثمانية أدوار يجتاز السلسلة كاملة. إزالة الـoverride هي النصف الذي يملكه النظام من المشكلة؛ النصف الآخر (توزيع الأدوار على أشخاص) قرار إداري.

**أثر التشغيل:** إن كان مسؤول النظام يعتمد فعليًا اليوم، سيتوقف عن ذلك بعد تطبيق الـmigration. الحل منح الدور التجاري صراحةً لمن يملك القرار — لا إعادة الـoverride. راجع `docs/security/separation-of-duties-2026-08-06.md`.

---

## 2026-08-10 — D14: اختبارات العقد تثبّت السلوك لا آلية التنفيذ
**القرار:** اختبار العقد يؤكّد ما يراه المستخدم أو ما تضمنه الوحدة، لا الطريقة التي كُتب بها الكود. مُنع تثبيت أسماء الدوال أو شكل الاستدعاء إلا حين يكون الشكل نفسه هو الضمانة (مثل `shouldFilter={false}` الذي يمنع عودة عيب معروف).

**السبب:** `phase2-page-consolidation.contract.test.ts` كان يؤكّد `beforeLoad: () => { throw redirect(...) }` حرفيًا. تلك الآلية بعينها هي التي سبّبت شاشة بيضاء على `/boq` و`/rfq-jih`، فصار الاختبار يحرس العيب ويُفشل الإصلاح. اختبار يمنع تصحيح خطأ أسوأ من غياب الاختبار.

---

## 2026-08-10 — D13: CSP مفروضة لا مراقِبة
**القرار:** ترويسة `Content-Security-Policy` تُرسَل مفروضة في `src/server.ts`، مع إبقاء نسخة `Report-Only` بنفس التوجيهات لتجربة أي تشديد لاحق قبل فرضه.

**السبب:** السياسة كانت `Report-Only` فقط ومن دون `report-uri` أو `report-to` — فلا تمنع خرقًا ولا تُبلغ عنه أحدًا. كانت تعطي مظهر ضبط أمني بلا أي أثر. التوجيهات لم تتغيّر (هي ذاتها التي عملت في وضع المراقبة)، وتم التحقق حيًّا من خلوّ الكونسول من أي مخالفة قبل الفرض.

**أثر التشغيل:** أول نشر بعد هذا التغيير يفرض السياسة فعليًا. أي مصدر خارجي جديد (سكربت، خط، نطاق API) يجب إضافته إلى التوجيهات وإلا سيُحجب في الإنتاج.

---

## 2026-08-06 — D12: المدير المالي ومدير التقدير شخص واحد (Ahmed Zaid) — ولا يُفرَض فصلهما
**القرار:** `finance_manager` و`estimation_manager` دوران منفصلان في النظام لكنهما **وظيفة شخص واحد فعليًا في PHC: Ahmed Zaid**. لا يُضاف أي قيد يفرض أن يكون منفّذ خطوتَي BAFO (cost_approval وfinance_review) شخصين مختلفين.

**السبب:** الأدوار تراكمية، وtrigger سلسلة BAFO (`20260727220000`) يفرض **الدور والترتيب فقط** لا تمايز الأشخاص — فحامل الدورين ينفّذ الخطوتين بالتتابع بشكل سليم. فرض شخصين مختلفين كان سيعني اختراع مُعتمِد ثانٍ لا وجود له في الشركة، أي تعطيل تدفق حقيقي لأجل ضبط صوري.

الخطوتان مختلفتان وظيفيًا رغم وحدة الشخص: خطوة التقدير تسأل «هل السعر فوق تكلفتنا؟» وخطوة المالية تسأل «هل يناسب هامشنا وسيولتنا؟». الفصل الحقيقي يبقى في **الخطوة 4 (تنفيذي)** — وهي العين الثانية على قراري Zaid.

**كيف يُستخدم:** السلسلة أربع خطوات لكن **ثلاثة أشخاص متمايزين** عمليًا: مُقدِّم الطلب ← Zaid (خطوتان) ← تنفيذي. لا تصف الوثائق السلسلة كأربعة فحوص مستقلة. ولا تُضِف قيد `cost_approval_by <> finance_review_by`.

**ملاحظة مترتبة (تحتاج قرار المستخدم، لم تُنفَّذ):** Ahmed Zaid **لا حساب له في النظام حتى الآن**، والدوران يحملهما حاليًا حساب آخر يحمل ثمانية أدوار — انظر تقرير فصل المهام في `docs/security/`.

---

## 2026-08-05 — D11: نموذج إدخال واحد يوجّه نفسه؛ نموذج "New RFQ" المنفصل حُذف
**القرار:** نموذج واحد فقط لكل ما يدخل النظام — `NewIntakeDialog` — مُركَّب في ترويسة `AppShell` (يُفتح من أي صفحة) وفي صفحة الاستقبال، بنفس المكوّن لا بنسخة ثانية. عند الحفظ: يُستنتج التصنيف من الحقول ([[D8]])، ثم **يُوجَّه العنصر مباشرة**:
- `project_type = jih` + اسم مشروع ← فرصة عند `rfq_received` + RFQ مربوط + جهة اتصال + شركة + متابعة + نشاط، والانتقال لصفحة الفرصة.
- `project_type = tender` + اسم مشروع ← مناقصة في لوحة المراقبة، والانتقال إليها. **بلا فرصة** (§3 و§27: المناقصة لا تُحتسب في أنابيب JIH قبل التحويل).
- بلا نوع أو بلا اسم ← يبقى `unclassified` في الاستقبال للفرز اليدوي.

**حُذف `NewRfqDialog.tsx`** ومفاتيحه في i18n بالكامل.

**السبب:** طلب صريح من المستخدم (2026-08-05). كنت قد بنيت نموذج "+ New RFQ" منفصلًا استجابةً لـ§6/§24، لكن نموذجين يغطيان نفس الأرض أسوأ من نموذج واحد يوجّه نفسه — ونموذج الاستقبال **يحمل بالفعل كل حقول §24 وأكثر**. أما شرط §6 (نقطة إنشاء متاحة من كل مكان) فيتحقّق بتركيب النموذج الواحد في الترويسة، لا ببناء ثانٍ بجانبه.

النتيجة أن §25 كلها تقع خلف حفظة واحدة: إنشاء الفرصة (2)، التصنيف (3)، سجل النشاط (4)، المتابعة الافتراضية (17)، وإنشاء/ربط جهة الاتصال (13) والشركة (14).

**كيف يُستخدم:** لا تُضِف نموذج إنشاء ثانيًا. أي حقل جديد يُطلب عند الإدخال يُضاف إلى `newIntakeFields` في `src/components/phc/NewIntakeDialog.tsx` — وهو المصدر الوحيد لقائمة الحقول الآن. خطوتا "تصنيف" و"تحويل" اليدويتان تبقيان في صفحة الاستقبال للعناصر التي لم تُوجَّه تلقائيًا فقط.

## 2026-08-05 — D10: التحويل من الاستقبال يُنتج **فرصة**؛ مشروع الإنتاج لا يُنشأ إلا عند الفوز
**القرار:** `convertInboxToRfq` تُنشئ الآن **الفرصة** (عند `rfq_received`) وسجل RFQ مربوطًا بها وجهة الاتصال والشركة ومتابعة وسجل نشاط — عبر `createRfqWithOpportunity`. عنصر الاستقبال يُشير إلى **الفرصة** (`converted_record_type = "opportunity"`)، والواجهة تنتقل إلى صفحة الفرصة بعد التحويل. **حُذف خيار "إنشاء مشروع جديد" من نافذة تحويل RFQ نهائيًا**؛ بقي ربط اختياري بمشروع **قائم** فقط.

**السبب:** كانت الدالة تستدعي `createRfq` وتتوقف — فالتحويل يُنتج صف RFQ **ولا شيء غيره**: لا فرصة، فلا شيء يظهر في Pipeline → Opportunities، ولا سجل يمكن تمريره عبر المراحل. الشيء الوحيد الذي كان يراه المستخدم بعد التحويل هو المشروع الذي أجبرته النافذة على إنشائه — ولهذا ظهر استفسار مبيعات كمشروع في قسم الإنتاج (تقرير فيصل الميداني، الجولة الثانية).

المواصفة تحسم الموضع الصحيح لكل خطوة:
- **§25** بند 2 "Create the opportunity" وبند 10 "Add the opportunity to the correct pipeline" — عند حفظ الـRFQ.
- **§29** تضع إنشاء المشروع في الطرف الآخر تمامًا، تحت Awarded: "Create project handover".

والمستودع يُنفِّذ الطرف الآخر بشكل صحيح أصلًا: trigger `create_project_from_won_opportunity` يبني مشروع الإنتاج عند وصول الفرصة إلى `won`.

**ضرر ثانٍ أخفى:** ذلك الـtrigger مشروط بـ`WHEN NEW.project_id IS NULL`. ففرصة أُعطيت مشروعًا عند الاستقبال **لا تحصل أبدًا على مشروعها الصحيح بعد الترسية** — المشروع السابق لأوانه يحلّ محله صامتًا.

**كيف يُستخدم:** ترتيب دورة الحياة: استقبال ← (تصنيف مستنتَج، [[D8]]) ← تحويل ← **فرصة في Pipeline** ← مراحل ← `won` ← **مشروع الإنتاج تلقائيًا**. لا تُعِد إضافة إنشاء مشروع لأي خطوة قبل الفوز. الربط بمشروع قائم يبقى متاحًا لحالة §39 (عدة مقاولين مسعَّرين على مشروع واحد).

## 2026-08-05 — D8: التصنيف يُستنتج من نموذج الاستقبال، والتصنيف اليدوي يبقى للحالات الغامضة
**القرار:** عند حفظ إدخال جديد، إن حُدِّد `project_type` (jih|tender) **و**اسم المشروع، يُصنَّف العنصر تلقائيًا (`jih → rfq`، `tender → tender`) وتصبح حالته `in_review` جاهزًا للتحويل. إن نقص أحدهما يبقى `unclassified` وتُطلب خطوة التصنيف اليدوي.

**السبب:** المواصفة §25 تُعدِّد 18 إجراءً يجب أن يقع **تلقائيًا** عند حفظ الـRFQ، البند الثالث منها نصًّا: "Classify it as Tender or JIH". النموذج يلتقط هذا التمييز أصلًا في `project_type`، فسؤال المستخدم عنه مرة ثانية بنافذة منفصلة هو إعادة إدخال بحتة. فيصل (مندوب مبيعات، 2026-08-05): "مرحلة الـclassifying لا داعي لها لأني وضّحت التفاصيل في النموذج".

**لماذا `rfq` وليس تصنيفًا اسمه "jih":** مسار JIH في هذا النظام يُدخَل عبر سجل RFQ — `convertInboxToRfq` هو مسار JIH و`convertInboxToTender` مسار المناقصة. يتوافق مع §4.2 التي تسمي أول مرحلة JIH: "New JIH RFQ". انظر [[D6]].

**كيف يُستخدم:** `inferClassification()` في `src/lib/inbox-actions.ts` + اختبارات في `intake-flow.contract.test.ts`. **التصنيف اليدوي لم يُحذف** — قائمته العشر (company/contact/duplicate/incomplete/signal_watchlist…) تبقى لتوجيه ما ليس صفقة، وهو غرضها الحقيقي.

## 2026-08-05 — D9: "+ New RFQ" مسار دخول مباشر بجانب الاستقبال، لا بديلًا عنه
**القرار:** زر "+ طلب جديد" في ترويسة `AppShell` (ظاهر بكل الصفحات، مقيَّد بـ`canCreateSalesRecords`) يفتح نموذجًا واحدًا يُنشئ عبر `createRfqWithOpportunity`: الشركة وجهة الاتصال (إنشاء أو ربط) + الفرصة عند `rfq_received` + سجل RFQ **مربوط بالفرصة** + متابعة بعد 3 أيام + سجل نشاط أول. ثم ينتقل بالمستخدم إلى الفرصة المُنشأة.

**السبب:** المواصفة §6 تشترط زر "+ New RFQ" يبقى "visible or easily accessible throughout the application"، و§24 تعرّف حقوله، و§25 تشترط أن يُنتج الحفظ الحالة الابتدائية كاملة، و§45-1 تشترط إنشاء RFQ في أقل من دقيقتين. الواقع قبل ذلك: أربع شاشات (استقبال ← تصنيف ← تحويل ← تحويل RFQ لفرصة)، وناتجها RFQ **غير مربوط** بفرصته إطلاقًا (`rfqs.opportunity_id` كان يبقى NULL — تحقَّقنا حيًّا على `RFQ-2026-0001`).

`createRfqWithOpportunity` كانت مبنية منذ فترة **بلا أي مستدعٍ** — لم تُبنَ دالة جديدة، بل رُبطت ونُقص منها الربط بالفرصة فأُضيف.

**كيف يُستخدم:** **الاستقبال لم يُلغَ** — يبقى قمع الفرز لما ليس RFQ معروفًا بعد (إشارات سوق، إدخالات ناقصة، تكرارات، عناصر تتبيّن شركة أو جهة اتصال). الزر الجديد للمسار الذي يعمل به العمل فعلًا: RFQ وصل، بموعد نهائي، من مرسل معروف. **القيمة التقديرية غير موجودة بالنموذج عمدًا** — قرار العميل 2026-08-03 جعلها حقلًا ماليًا لاحقًا (`canEditTotalValue`).

## 2026-08-05 — D7: تصحيح إدخال الاستقبال مسموح قبل التحويل فقط
**القرار:** `updateInboxItem()` جديدة تسمح بتعديل أي حقل بسجل استقبال **لم يُحوَّل بعد** (بما فيه `project_type`). بعد التحويل: `classifyInboxItem` و`markConverted` كلاهما يرفض برسالة صريحة تُحيل لتعديل السجل الناتج نفسه.

**السبب:** كان سجل الاستقبال **write-once** — لا وجود لأي دالة تعديل، والحقلان الوحيدان القابلان للتغيير هما `classification` و`status`. فخطأ إملائي باسم مشروع أو مقاول أو موعد غير قابل للتصحيح إطلاقًا. وطلب فيصل تحويل عنصر مصنَّف RFQ إلى Tender يعني — بعد [[D8]] — تعديل `project_type`، وهو ما كان مستحيلًا.

بالمقابل، إعادة التصنيف **بعد** التحويل كانت ستترك السجل الناتج (rfq/tender/project…) يتيمًا: `markConverted` كان يستبدل `converted_record_id` بلا فحص. المواصفة §40 تشترط صراحة "Preserve the original RFQ" و"Record who converted it".

**كيف يُستخدم:** لا تُضِف مسار "تراجع عن التحويل". المسار الصحيح لتصحيح تصنيف بعد التحويل: افتح السجل الناتج وعدِّله هناك، أو أرشِف عنصر الاستقبال وأنشئ غيره.

## 2026-08-05 — D6: مرحلة البداية لأي فرصة جديدة هي `rfq_received` وليست `jih`
**القرار:** كل مسار يُنشئ فرصة يضبط `sales_stage = 'rfq_received'`. يشمل ذلك المسارين اللذين كانا يتركانه `NULL` قبل PR #169: `createOpportunityForCompany` (زر New Opportunity بصفحة الحساب) و`convert_lead_to_opportunity`. **لا يُستخدم `jih` كمرحلة إنشاء إطلاقًا** — يُوصَل إليها فقط بانتقال صريح من `rfq_received`.

**السبب:** محسوم بمواصفة العميل (وثيقة إعادة تصميم لوحة المبيعات، 46 قسمًا) — ليس اجتهادًا:
- **§4.2** أول مرحلة في مسار JIH اسمها حرفيًا **"New JIH RFQ"** (ومقابلها "New Tender RFQ" في مسار المناقصات). أي أن الفرصة تبدأ عند الـRFQ، لا بعده.
- **§25** "When a new RFQ is saved, automatically: … 2. Create the opportunity. 3. Classify it as Tender or JIH." — إنشاء الفرصة **نتيجة** لحفظ RFQ.
- **§33** "The Business Development module must track opportunities **before an RFQ is received**" ثم "**When an RFQ is received**, convert the BD lead into an RFQ" — النشاط السابق للـRFQ مكانه وحدة BD كـ`lead`، لا كفرصة. فتحويل Lead إلى فرصة يقع **لحظة** استلام الـRFQ، أي عند `rfq_received` بالضبط.

بعبارة أخرى: بحسب المواصفة **لا توجد فرصة قبل وجود RFQ**، فلا يصح أن تُولد فرصة عند `jih` — تلك مرحلة تالية تعني أن العمل التقديري/الفني بدأ فعلًا.

**كيف يُستخدم:** مفروض باختبار عقد `src/lib/opportunity-sales-stage.contract.test.ts` الذي يحلّل جسم كل `INSERT` في المواقع الخمسة ويسقط إن أغفل أحدها العمود أو استخدم قيمة أخرى. لا تُعِد فتح هذا كسؤال منتج — الجواب في المواصفة نصًّا.

**ملاحظة مترتبة (لم تُنفَّذ):** بحسب نفس المنطق، زر "New Opportunity" بصفحة الحساب يُنشئ فرصة بلا RFQ مقابل — وهو ما لا تعرّفه المواصفة. إمّا أن يُطلب RFQ ضمن نفس التدفق، أو يُعاد تصنيفه كـBD lead. بند مفتوح في `tasks/backlog.md`.

## 2026-07-26 — D2: توسيع مفردات leads.source وترك owner_id فارغًا مقصود
**القرار:** `leads.source = 'import'` قيمة رسمية معتمَدة الآن إلى جانب `protenders | external | manual` الأصلية (`20260707100030_leads.sql`). كما أن ترك `owner_id` فارغًا (`NULL`) عند إنشاء leads من الخادم (commit_candidates وrun_protenders_ingest) **مقصود** — لا يُعيَّن مالك افتراضي تلقائيًا، والتعيين يتم لاحقًا من قِبل مستخدم بشري.
**السبب:** توثيق فقط لسلوك قائم فعليًا في `_shared/leads.ts::insertLeadServerSide()` منذ PR #119 — لا حاجة لتغيير الكود أو البيانات.
**كيف يُستخدم:** موثَّق أيضًا عبر `COMMENT ON COLUMN` في migration `20260726100000_document_leads_source_owner_id.sql` (توثيقية بحتة، بلا تغيير سكيما). لا تُعِد فتح هذا كسؤال منتج معلّق.

## 2026-07-26 — D1: توسيع مطابقة الأسماء العربية لتشمل project_name وmain_contractor
**القرار:** مطابقة التكرار (duplicate detection) في `import-pipeline` (`compareSignals()`، `_shared/import-dedup.ts`) تُوسَّع لتقارن `main_contractor` أيضًا (بجانب `company_name` وproject_name الموجودَين مسبقًا)، باستخدام نفس `normalizeCompanyName()` الموحَّدة من D1. كانت `DedupSignals.main_contractor` مُعرَّفة في النوع ومُمرَّرة من `import-pipeline/index.ts` لكن لم تُقارَن فعليًا — فجوة صامتة أُغلقت الآن.
**السبب:** `duplicates.ts` (محرك كشف التكرار العام) كان يقارن الحقول الثلاثة (name/project_name/contractor_name) بالفعل؛ `import-dedup.ts` (مسار الاستيراد) كان متأخرًا وناقصًا لحقل واحد فقط.
**كيف يُستخدم:** موثَّق باختبار جديد في `src/lib/import-dedup.test.ts` ("main_contractor match is detected"). confidence=0.65، reason_code=`same_main_contractor`، suggested_action=`needs_manual_review` — أقل من project_name (0.7) عمدًا لأن اسم المقاول وحده أضعف إشارة تكرار من اسم المشروع.

## 2026-07-23 — تأجيل الفحوصات الوظيفية وUAT بعد نشر D1/D2/D5
**القرار:** بعد نشر migration D5 وEdge Functions المتأثرة (`import-pipeline`, `sales-os-api`) إلى الإنتاج، تأجيل الفحوصات الوظيفية وUAT (تتطلّب كتابة بيانات اختبار في CRM حي) — غير عاجلة، تُنفَّذ لاحقًا وليس فورًا.
**السبب:** الـ preflight وفحوصات الصحة الآلية (migration lint نظيف، نسخ Edge Functions صحيحة، 401 متوقَّع على الطلبات غير المصادَقة) كانت كافية لتأكيد أن النشر لم يكسر شيئًا. المستخدم قرّر أن الفحص العميق بمستخدمين حقيقيين ليس أولوية فورية.
**كيف يُستخدم:** لا تُعِد افتراض أن هذا البند "معلّق بشكل حرج" في جلسات لاحقة — هو في `tasks/backlog.md` بانتظار وقت مناسب، وليس حاجزًا أمام أي عمل آخر.

## 2026-07 — النشر مبوّب بموافقة بشرية
**القرار:** لا نشر تلقائي لموارد Supabase أو Cloudflare عند دمج `main`؛ النشر عبر GitHub Actions dispatch يدوي داخل بيئة `production-cloudflare` المحمية.
**السبب:** حماية بيانات CRM الحية ومنع نشر غير مقصود. المرجع: `docs/deployment-governance.md`.

## 2026-07 — بوابة AI واحدة (ai-orchestrator)
**القرار:** كل وكلاء AI خلف Edge Function واحدة backend-only. ممنوع الاستدعاء المباشر من الـ frontend أو إضافة Edge Function لكل وكيل.
**السبب:** توحيد الحُرّاس (guardrails)، منع تسريب المفاتيح، ونقطة تحكم واحدة. المرجع: `docs/ai-orchestrator.md`.

## 2026-07 — عزل مشروعَي Supabase
**القرار:** الإنتاج على `lrfdtoexyeghrzynapyn` فقط؛ المشروع القديم `xpoduufwoklvsbuhywsv` لا يُعدَّل إطلاقًا.
**السبب:** فصل صارم يمنع الكتابة على بيانات قديمة/حساسة.

## 2026-07 — Cloudflare Workers مع إبقاء Lovable fallback
**القرار:** الإنتاج على Cloudflare Worker `mophc-sa-phc-command-center`؛ يبقى `lovable-fallback` واستضافة Lovable متاحين حتى تنجح إصدارتان إنتاجيتان متتاليتان في Production Readiness.
**السبب:** انتقال آمن دون انقطاع.

## 2026-07 — Bun كمدير حزم/تشغيل
**القرار:** Bun 1.3.14 (`bun.lock`, `bunfig.toml`) بدل npm/pnpm.
**السبب:** سرعة وتوحيد بيئة التطوير والاختبار.

## 2026-08-19 — Phase 4: الإجراءات طبقة قراءة، والإشعارات جدول مستقل
**القرار:** لم نُنشئ جدول `actions` موحّدًا. الإجراءات تبقى في مصادرها الخمسة
(`opportunity_flags` · `tasks` · `follow_ups` · `approvals` · بوابة المراجعة في `inbox_items`)
ويُسقِطها `src/lib/action-center.ts` في شكل واحد للقراءة فقط. في المقابل أُنشئ جدول
`notifications` حقيقي بـ RLS وعزل مستلم و`dedupe_key`.
**السبب:** الدمج الفيزيائي كان سيعني إعادة كتابة خمس آلات حالة تعمل وترحيل صفوف حيّة
دون أي مكسب فوق طبقة القراءة. أما الإشعارات فكانت مُشتقّة وقت القراءة، وهو ما يجعل
"مقروء/غير مقروء" مستحيلًا ويعيد عرض نفس البند كل جلسة — لذلك احتاجت جدولًا.

## 2026-08-19 — بصمة الحدث (`dedupe_key`) لا الطابع الزمني
**القرار:** الإشعار يُعرَّف بـ (المستلم + النوع + الكيان + بصمة الحالة). نفس البصمة = نفس
الحدث = لا إشعار جديد، مهما تكرّر الحفظ. القيد مفروض بـ UNIQUE INDEX في القاعدة لا في
دالة الإرسال.
**السبب:** نفس منطق `condition_key` المُثبت على `opportunity_flags` (2026-08-06). الاعتماد
على التواريخ اليومية كان سيُنتج تنبيهًا يوميًا عن حالة قائمة لم تتغيّر — وهو ما يُدرّب
المستخدم على تجاهل الجرس.

## 2026-08-19 — بوابة المراجعة على `inbox_items` لا `opportunities`
**القرار:** محفّزات إشعارات الـintake تعمل على `inbox_items`.
**السبب:** بوابة Phase 2 تعمل *قبل* أن يصبح الطلب فرصة — وهذا هو الغرض منها. وضع
المحفّز على `opportunities` كان سيعني ألا يصدر أي إشعار intake إطلاقًا. اكتُشف الخطأ
أثناء تشغيل الهجرة على Postgres مؤقت.

## 2026-08-20 — الإشعار المتأخر داخل run_sales_automations لا في مجدول ثانٍ
**القرار:** `notify_overdue_items()` تُستدعى من داخل `run_sales_automations` في نهايتها،
لا عبر cron job منفصل.
**السبب:** هذه الدالة هي المحرّك الدوري القائم (pg_cron ليلًا + زر Action Center عبر
sales-os-api). مجدول ثانٍ كان سيمنح المسارين سلوكًا مختلفًا — وهو بالضبط ما أُنشئت
20260806120000 لمنعه حين جعلتها المصدر الوحيد للقواعد. واستدعاؤها **بعد** القواعد
مقصود: القاعدة قد ترفع للتو علمًا تجاوز تاريخه، فيُشعَر في نفس التمريرة لا بعد يوم.

## 2026-08-20 — نافذة استرجاع 7 أيام لإشعار التأخير
**القرار:** لا يُشعَر إلا عن بند تأخّر خلال الأيام السبعة الماضية.
**السبب:** «أصبح متأخرًا» انتقال، لكن صف العلم يحمل تاريخ استحقاق فقط — لا حدث للحظة
التجاوز. مسح بلا حدود يعامل كل بند متأخر تاريخيًا كأنه تأخّر اليوم: غير صحيح، ويُفرغ
الإرث كله دفعة واحدة في أول تشغيل. التشغيل الليلي يلتقط أي انتقال حقيقي خلال يوم،
فالنافذة تحتاج فقط لاستيعاب تشغيلات فائتة.

## 2026-08-20 — إصلاح enum مفقود أوقف محرّك الأتمتة ١٤ يومًا
**القرار:** إضافة `submission_pending_on` إلى `queue_action_type`، وإضافة cast صريح
لـ`priority_tier` في القاعدة 12، مع اختبار عقد يربط كل قيمة تستخدمها قواعد SQL
بالـenum الذي تعرّفه الهجرات.
**السبب:** هجرة 20260806140000 أضافت القاعدة 12 بقيمة enum لم تُضَف قط. العمود والفهرس
في نفس الهجرة طُبِّقا بنجاح فبدا كل شيء سليمًا — القيمة لا تُحلّ إلا عند تنفيذ الـINSERT.
منذ 2026-08-07 فشل كل تشغيل ليلي عندها، وبما أن الدالة معاملة واحدة تراجعت القواعد
1–11 معها: لم يرفع النظام أي إجراء لأربعة عشر يومًا. اكتُشف الأمر أثناء ربط إشعار
التأخير — لأن ربط إشعار بدالة تنهار ليلًا كان سيسلّم حدثًا لا يقع أبدًا.

## 2026-08-20 — KPI واحد قانوني، وكل رقم يشرح نفسه
**القرار:** كل مؤشرات المبيعات تُحسب في `src/lib/sales-kpis.ts` عبر
`resolveCanonicalStage`، والـKPI ليس رقمًا بل كائن يحمل الصيغة والمصدر والفلاتر
وقائمة معرّفات السجلات وهدف الـdrilldown.
**السبب:** كانت المؤشرات تُحسب في ثلاثة أماكن بثلاثة تعريفات: `targets-metrics`
يفلتر على enum القديم، `dashboard-helpers` يعدّ Won من `stage`،
و`command-center` يخلط العمودين. النتيجة أن صفقة في `jih_bafo` لم تكن محسوبة في
أي مكان. وربط الصيغة بالرقم في نفس الكائن يمنع انحراف الـtooltip عن القيمة.

## 2026-08-20 — الاحتمال غير المعروف يُستبعد ولا يُفترض
**القرار:** الفرص بلا احتمال تُستبعد من الـWeighted Pipeline ويُذكر عددها، ولا
تُوزن بقيمة افتراضية. و`human_win_probability` تسبق AI ولا يُدمجان أبدًا.
**السبب:** التطبيق السابق كان يطبّق وزن 0.2 على كل فرصة غير مسجّلة — أي أنه
يصنع توقعًا من الجهل، ولا يمكن تمييزه لاحقًا عن تقدير حقيقي بنسبة 20%.

## 2026-08-20 — NO BOQ, NO PROJECT NUMBER — على `projects` لا على `inbox_items`
**القرار:** الحكم يسري على `projects.project_number` (PRJ-) فقط. أما
`inbox_items.project_number` (INT-) فهو مرجع التقاط للطلب الوارد ويبقى كما هو.
الرقم يُصدر عبر `issue_project_number()` وبوجود BOQ حالته
`verified` أو `partially_verified`، مع trigger يمنع أي مسار كتابة آخر.
**السبب:** الهجرة 20260803130000 تنص صراحة أن التقاط الوارد ليس مشروعًا مؤكدًا.
و`has_boq` مجرد ادعاء لا دليل، فلا يفتح الرقم. **تنبيه:** الإنتاج اليوم يحوي
صفر BOQ وصفر ارتباط project↔opportunity، لذا أي مشروع جديد بعد التطبيق سيبقى
بلا رقم حتى تُبنى تسجيل الـBOQ في Phase 7 — وهذا سلوك مقصود يحتاج تأكيدك.

## 2026-08-20 — الأداء يُعرض ولا يُسجَّل كنقاط
**القرار:** لوحة الفريق تعرض نشاط كل عضو دون score أو ترتيب حسب الإنتاجية؛
الترتيب حسب قيمة خط الأنابيب فقط.
**السبب:** عدّ المهام يقيس الكتابة لا البيع، وأي رقم ظاهر يتحول فورًا إلى هدف
يُحسَّن على حسابه. القيمة حقيقة عن العمل، لا حكم على الشخص.

---

## 2026-08-21 — إدارة النظام ليست سببًا لقراءة المستندات التجارية [[D24]]
**القرار:** `can_read_attachments()` قائمة أدوار مكتوبة يدويًا، وليست
`can_view_all_sales_data()` — الأخيرة تضم `system_admin` و`viewer`.
**السبب:** من يدير الحسابات والأدوار لا يحتاج قراءة عقود وBOQ، ومن له اطّلاع
للقراءة على أرقام خط الأنابيب لا يحتاج الملفات نفسها. إعادة استخدام الدالة
الموجودة كان سيبدو اتساقًا وهو في الحقيقة توسيع صامت للصلاحية. هذا امتداد
لفصل صلاحية الاعتماد التجاري عن الإدارة التقنية المُقرَّر سابقًا.

## 2026-08-21 — المسار يُخزَّن، والرابط يُصنع عند الضغط [[D25]]
**القرار:** العمود يحمل مسار التخزين؛ الرابط الموقَّع يُصنع لحظة ضغط المستخدم
عبر `AttachmentLink`، بعمر 10 دقائق، ولا يُخزَّن أبدًا.
**السبب:** الرابط الموقَّع مفتاح مؤقت لا عنوان. حين كان عمره 7 أيام صار طويلًا
بما يكفي ليبدو جديرًا بالتخزين — وهكذا وصل إلى قاعدة البيانات وقتل ملفين.
عشر دقائق تكفي لتعيش النقرة ولا تغري أحدًا بحفظها.
**والأهم:** الشقّان — التخزين والقراءة — تغيير واحد لا اثنان. تخزين المسار
وحده يترك مواضع القراءة تُنتج روابط ميتة، والأدلة الجديدة بلا رابط إطلاقًا.

## 2026-08-21 — المرجع الذي لا يُحلّ يُبلَّغ عنه ولا يُخمَّن
**القرار:** `rerun_attachment_backfill()` تسترجع المسار فقط إذا استُخرج بلا
تخمين **و** وُجد كائن بهذا الاسم. كل ما عداه يذهب إلى
`document_backfill_report` بسبب مكتوب، والكائن اليتيم يُبلَّغ عنه ولا يُحذف.
**السبب:** على هذه البيانات بالذات، backfill ساذج كان سيكتب
`no-reply@raseedinvest.com` في عمود مسار تخزين ويتبنّى رابطي Drive كمستندين
داخليين. ملف غير مُشار إليه هو على الأرجح رابط ضائع لا نفاية، وحذفه يُتلف
الدليل اللازم لإعادة وصله.

## 2026-08-22 — صلاحية المستند اقتران، لا اشتقاق من RLS الكيان [[D26]]
**القرار:** `document_entity_grants()` تُعدّد الكيانات صراحةً وتشترط:
مصلحة شخصية في السجل **أو** (دور مستندات من قائمة D24 **و** وجود السجل).
ليست دالة SECURITY INVOKER تدع سياسة كل جدول تقرّر.
**السبب:** قِست السياسات الفعلية على الإنتاج قبل التصميم — `projects`
و`inbox_items` مفتوحتان لكل مستخدم نشط، و`contracts` سياستها `true`،
و`can_view_all_sales_data()` تضم `viewer` و`system_admin`. الاشتقاق «الأنيق»
كان سيسلّم `viewer` كل عقد وBOQ في النظام. الصراحة هنا أأمن من إعادة الاستخدام،
وثمنها أن كل نوع كيان جديد يحتاج فرعًا — و`ELSE FALSE` يجعل النسيان يفشل مغلقًا.

## 2026-08-22 — لا حذف فيزيائي، ولا ربط بلا رؤية الطرفين
**القرار:** لا سياسة DELETE على `documents` ولا على `document_links`؛ الحذف
الناعم يوقف تقديم البايتات ويُبقي الصف والكائن. وسياسة INSERT على الروابط
تشترط أن تقرأ المستند **وأن** تصل إلى الكيان.
**السبب:** جدول روابط يمنح الصلاحية بالخطأ افتراضيًا — اربط ملف غيرك بفرصة
تملكها فتقرأه. الشرط المزدوج يغلق ذلك، و`trigger` يجمّد `storage_path` ليغلق
الطريق الآخر: تملك صفًّا ثم توجّهه لملف غيرك.

## 2026-08-22 — الموقع على المشروع والصورة فقط [[D27]]
**القرار:** إحداثيات على `projects` (الموقع الفعلي) وعلى `documents` (نقطة
التقاط الصورة). لا على الفرص ولا الواردات ولا الشركات. NUMERIC(9,6)، بلا PostGIS.
**السبب:** المخطط يحمل `location` نصيًّا في ستة جداول أصلًا؛ الناقص هو
الإحداثيات، وهي تستحق الوجود حيث يُزار المكان فعلًا. الفرصة تصير مشروعًا،
فإحداثياتها ستكون نسخة ثانية تحتاج مزامنة. وصورة اللافتة عند المدخل الشمالي
نقطتها ليست نقطة المشروع — لذلك هي على الصورة لا على المشروع. PostGIS يشتري
استعلامات مسافة لا يطلبها شيء بعد.

## 2026-08-24 — ترقية السجل التاريخي: `rfq_received` ثم انتقال معلن [[D6]]
**القرار:** `promote_historical_row()` تُنشئ الفرصة عند `rfq_received` ثم تنقلها
بانتقال صريح مُسجَّل في `stage_transition_history` و`audit_log`. الحالات النشطة
الثلاث (SUBMITTED · WAITING FOR CLIENT · FOR ACTION) تصل جميعها إلى
`sales_stage = 'jih'` وتُفرَّق بـ`commercial_handoff_status`
(submitted · waiting_client · with_sales). القواعد بيانات في
`historical_sales_status_map`، لا `CASE` في الكود.
**السبب:** الدالة كانت تترك `sales_stage = NULL`، والعمود بلا DEFAULT — وكل
عروض المرحلة 10 تُرشِّح `NOT IN ('won','lost')` وNULL يسقط منها، بينما
`conversion_summary` تعدّها في `total_deals`؛ أي أن الحساب لا يتوازن. وهذا وقع
فعلًا: `crm-actions.ts:218` يسجّل «2 of 4 production opportunities were orphaned
this way» بتاريخ 2026-08-05. أما اختراع مرحلة `quotation_submitted` فيكرّر ما
يقوله `commercial_handoff_status` أصلًا — و20260818140000 ينصّ على أنه مستقل عن
المرحلة.

## 2026-08-24 — قيمة واحدة في عمود واحد، وحاسم واحد لكل القُرّاء
**القرار:** الترقية تكتب مبلغ الأرشيف في `quotation_value` فقط. و
`computeJihPipelineTotal()` صارت تستدعي `opportunityValue()` المشتركة بدل جمع
`estimated_value_max` وحده.
**السبب:** كان ذلك القارئ الوحيد المخالف في النظام، وهو الذي يغذّي **My
Workspace** — شاشة مندوب المبيعات نفسه. صفقة بقيمة معروضة بلا تقدير كانت تُقرأ
صفرًا هناك وكاملة في Sales Management. نسخ الرقم إلى عمود ثانٍ لإرضاء قارئ واحد
هو بالضبط كيف تبدأ لوحتان بالإبلاغ عن رقمين لنفس خط الأنابيب.

## 2026-08-24 — الإلغاء عبر `stage='archived'`، والأرشيف لا يُمَسّ
**القرار:** `void_historical_promotion()` تؤرشف الفرصة بـ`stage='archived'`،
تُنهي عرض السعر التاريخي (`expired`)، وتنقل الطلب إلى `voided` بسبب مكتوب. لا
عمود جديد ولا `sales_stage` جديدة. و`analytics_scope_opportunities` صارت تستثني
المؤرشف في موضع واحد، و`resolveCanonicalStage()` صارت تفحص `archived` **قبل**
`sales_stage`.
**السبب:** 20260711160000 تخطّى إضافة `archived_at` للفرص لأن «لديها أصلًا قيمة
`archived`»، و`record-lifecycle.ts` يرفض الحذف الفيزيائي لصالحها. لكن الفرع كان
يُفحص بعد `sales_stage`، فلا يُبلَغ أبدًا لصف حيّ — أي أن الحذف الناعم لم يكن
يُخرج الصف من الأرقام، وهو ليس حذفًا ناعمًا حينها.

## 2026-08-24 — عرض السعر التاريخي دليل، لا عرض حديث بقناع
**القرار:** السجل التاريخي يصير صفًّا في `quotations` بعلامة `is_historical`
و`historical_row_id`، ثابتًا بـtrigger. لا يُنشأ له `quotation_revisions` أبدًا.
المندوب يكمل العمل برفع مراجعة 7C حديثة فوقه، تمرّ بالبوابات كأي غيرها.
**السبب:** مراجعات 7C مشروطة بـBOQ مجمّد وسعر داخلي معتمد من GM ومطابقة سعرية
تامة؛ الأرشيف لا يملك أيًّا منها، وتصنيعها لتجاوز البوابة تزوير للسجل التجاري.
وفي الوقت نفسه السجل **هو** عرض سعر — رقم وقيمة وحالة وتاريخ إصدار — وبدونه لا
يمكن عدّ «عروض الأسعار المقدَّمة» ولا يجد المندوب ما يكمل منه.

<!-- انسخ كتلة قرار جديدة أعلى هذا السطر -->

## 2026-08-26 — لا اقتطاع صامت: رقم مرجعي يُحسب على الدفتر كله أو يُعلن نقصه
كل استعلام يغذّي مؤشرًا مرجعيًا يُقرأ عبر `fetchAllRows()` حتى النهاية. السقف (10,000 صف) باقٍ لأن «اجلب كل شيء» بلا حدّ عطبٌ بذاته — لكن بلوغه يُرجع `complete:false` ويرفع تحذيرًا **فوق** الأرقام.
**السبب:** `.limit(200)` على الفرص كان يحسب خط الأنابيب والتوقّع والمعرَّض للخطر على أول 200 صف. عند 49 سجلًا ذلك هو الدفتر كله؛ عند 201 هو إجماليّ دقيق وخاطئ لا شيء على الشاشة يكشفه. رقم يُحسب بهدوء على 200 من 3,000 أسوأ من غياب الرقم، لأنه يبدو تمامًا كالرقم الصحيح.

## 2026-08-26 — «صانع القرار» يُجاب عليه في مكان واحد، وبثلاث حالات
`decisionMakerState()` في `src/lib/stakeholder-roles.ts` هي المصدر الوحيد. أي شاشة تسأل السؤال تستدعيها ولا تقرأ `contractor_decision_maker` مباشرة. الحالات: نعم · لا · **لا يمكن تحديده** — والأخيرة لا تُبلَّغ لأحد ولا تدخل «جودة البيانات».
**السبب:** كان `attention.ts` يقرأ العمود وحده و`stakeholder-roles.ts` يقرأ `role_code` أيضًا، فتؤكد لوحة العلاقات وجود صانع قرار على صفقة تُدرجها «جودة البيانات» بلا واحد. ودمج «غير معروف» في «لا» يُرسل مديرًا يبحث عمّا قد يكون موجودًا — وهكذا يفقد الطابور قارئه.

## 2026-08-26 — الواجهة تحتمل غياب عمودها، فلا يوجد ترتيب نشر صلب
`fetchStakeholders()` تطلب `role_code` وتعيد الطلب بدونه إذا ردّ PostgREST بـ400. القراءة البديلة ليست منقوصة: `effectiveRole()` ترجع لنص `role` التاريخي، وهي بالضبط قراءة ما قبل الـmigration.
**السبب:** التعليق الذي يوثّق ترتيب النشر ليس ضمانة. بدون هذا الاحتياط كان استعلام اللوحة كله يسقط قبل تطبيق `20260915100000` — **لوحة بيضاء لكل مستخدم**، لا رقم ناقص.

## 2026-08-26 — صلاحية تنفيذ الدالة تُفحص على المستدعي، لا على مالك الـview
دالة `SECURITY DEFINER` تُستدعى داخل view عادي تحتاج أن يملك **المستدعي** صلاحية التنفيذ. ملكية الـview تحكم الوصول إلى **الجداول** فقط.
**السبب:** بنيتُ `20260916100000` على العكس، فسحبتُ التنفيذ من `authenticated` وكسرتُ `pipeline_by_stage` و`sla_breaches` في الإنتاج لكل مستخدم. الحلّ ليس سحب الصلاحية بل أن تفحص الدالة الرؤية بنفسها.

## 2026-08-26 — لا تُكتب اختبارات صلاحية عبر `SET ROLE rls_tester`
الـharness ينفّذ `GRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA public TO rls_tester`. أي اختبار يسأل «هل يستطيع هذا الدور استدعاء الدالة؟» بتبديل الدور **سينجح على قاعدة مكشوفة ويقول إنها آمنة**.
**السبب:** حدث فعلًا. اسأل `has_function_privilege()` على الدور الحقيقي (`anon`/`authenticated`)، أو اختبر **الإجابة** تحت `test.uid` لأن المسند يقرأ `auth.uid()` لا الدور المتصل.

## 2026-08-26 — امتياز الاختبار لا يتسرّب إلى ما يُختبَر
`rls_tester` يرث صلاحياته من `authenticated`، ولا يتلقّى منحًا مباشرًا لا يملكه مستخدم حقيقي — للجداول **وللدوال** معًا. الاستثناء الوحيد `SELECT` على `auth.users` لحلّ معرّفات الـfixtures، وهو موثَّق ومحروس باختبار يؤكّد أن `authenticated` الحقيقي لا يملكه.
**السبب:** `GRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA public TO rls_tester` جعل مجموعة اختبارات خضراء بينما الإنتاج يردّ 42501. نصف القاعدة (الجداول) كان مطبَّقًا منذ البداية مع تعليق يشرحه؛ النصف الآخر (الدوال) لم يكن. **أي منح جديد لـ`rls_tester` يجب أن يُبرَّر بأنه إعداد لا امتياز دور.**
