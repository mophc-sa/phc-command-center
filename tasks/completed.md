# Completed — PHC Command Center

## 2026-09-07 — مكتمل: PR 285–287 وتفعيل البيانات المؤهلة

PR 287 دُمج ونُشر: `ba3dd43f083d063c32d620d23c31457b8e079fd4`، Worker `c6288e11-e6c1-406b-a10d-1d4d826d398b` بمرور 100%، `sales-os-api` v48 و`import-pipeline` v37، وترحيل `20260928140000`. جاهزية الإنتاج نجحت في run 34123345922؛ Preview URLs أُعيدت مغلقة. أخذت نسخة public-data قبل الترحيل.

تم تفعيل 80 فرصة و80 عرض سعر تاريخي بواسطة جلسة `moalagab@phc-sa.com` بعد أن أصبح دوره المخوّل متاحًا؛ الوكيل لم يمنح الدور. صفر رفض، SAR 150,723,240 دون ضريبة، المالك والقيمة والروابط متطابقة. الإجمالي 125 سجلًا مفعّلًا من 679؛ بصمة المصدر قبل/بعد متساوية. الدفعة الجديدة: 69 من 2025، 3 من 2024، 8 من 2023. بقي 554 سجلًا خارج مسار التفعيل بسبب الحالة أو النواقص أو التعارضات. لا تُفعّلها بتخمينات أو تغيير حالات الفوز والخسارة.

اختبار الواجهة: عرض جميع 679، مرشح undecided =103، unparsed code =110. إعادة فحص دفعة الاستيراد التجريبية أزالت اقتراحات التكرار المتراكمة (4→2) وحافظت على skip، والمراجعة النهائية نجحت، وملخص الفشل 0 written/1 failed بقي ظاهرًا بعد التحميل. لا كتابة CRM جديدة من اختبارات الاستيراد في هذه المرحلة.

متابعة العرض في `fix/activated-opportunity-display`: إظهار تصنيف الأرشيف في تفاصيل الفرصة، وحصر خيارات الإسناد الجديدة بالنشطين مع إبقاء أسماء المالكين السابقين. جاهزية هذا الإصلاح الإضافي تُقاس بصورة مستقلة قبل نشره.


## 2026-09-07 — إصلاح الفحص الشامل F01–F16 (الكود والتحقق المحلي)

- أُغلقت المسارات المثبتة للحسابات وMFA والموافقات وملكية الاستيراد؛ تثبيت ذري وسجل أصل وصور قبل/بعد وتراجع يفحص التعارض.
- قراءة كاملة للمؤشرات والاستيراد، إصلاح CSV وMCP، تثبيت SheetJS، ومقارنة صلاحيات كل توليفات الأدوار.
- تحقق محلي: 2656 اختبار Bun، 948 SQL/pgTAP، 13 Deno، فحص الخدمات الأربع والبناء؛ 147 تحذير lint قائمة و0 أخطاء.
- قواعد Protect Main صارت تتطلب الفحوص الستة، بما فيها الأمن وقاعدة البيانات. جاهزية الإنتاج تفشل عند غياب الأسرار أو تخطي الاختبارات.
- **غير منشور:** المهاجرات الثلاث، الخدمات الثلاث والواجهة تحتاج تسلسل الإطلاق الموثق. 11 اسم سر اختبار مفقود في البيئة والمستودع؛ لا تُنشأ قيم بديلة.
- الإصدار: `docs/audit-remediation-release.md`؛ الفرع `fix/system-audit-hardening`.


## 2026-09-06 — آخر مجموعتين من قائمة 2 سبتمبر، وثلاثة أعطال كشفها القياس
- **ز · التقويم (#281، منشور `b53224d`):** كان يقرأ ثلاثة مصادر من ستّة عشر. الأثر بعد ترشيح المُغلَق **17 ← 32 حدثًا**؛ صحّحتُ رقمي الأول (+37) إلى **+15** — تسعةٌ من اثني عشر متابعة طلبٍ كانت على طلباتٍ حُوِّلت. **وستّة من أسماء الأعمدة كانت خطأً في ذاكرتي** ولا يمسكها المترجم لأن الاستعلام نصّ.
- **ج · الترقيم (#282):** كود المبيعات يُسنَد عند إنشاء الحساب — **القاعدة مقروءة من الأربعة القائمة لا مخترَعة** (أول حرفين من الاسم الأول)، والتصادم ينمي حرفًا لأن قيدًا سابقًا يمنع الأرقام. ورقم المشروع يحمل مُدخِله.
- **عطلٌ حيّ لا ميزة ناقصة:** مديرة تطوير المبيعات كانت ترى زرّ التوكيل والخادم يردّ 403 — `canAssignOwner` مديرو تجارة فقط. والمنح الجديد أضيق من الزرّ: **بلا مالك يوكّلها أي مشغّل مسار؛ وذات المالك تبقى للمديرين.**
- **وتوكيل مدير المبيعات لأحد مندوبيه كان يعمل أصلًا** — قِيس ولم يُفترض.
- **والزناد الذي كتبتُه (#278) كان يرفض المدير وحده.** `auth.uid()` الفارغ لا يعني مُناديًا مجهولًا؛ و`anon` لا يملك `UPDATE` على `profiles`. **فحصٌ لا يرفض إلا صاحب الحق ليس حاجزًا.**
- PRs: #278 · #279 · #280 · #281 · **#282 (مفتوح، فيه مهاجرة)**.

## 2026-09-02 — اللوحة نُشرت، ورفع الملفات أُصلح

- **#261 · إعادة بناء اللوحة الجدارية** — مدموج `9fe405c`، **منشور ومقيس على الإنتاج**: حالات فارغة صادقة، سلّم مراحل يرمّز حدّ الالتزام التجاري، رقاقات موحّدة بالرقم يسارًا، وشريط أخبار متحرّك باتّجاه اللغة. ثمانية عيوب صامتة كشفها القياس — منها `text-amber-deep` غير المعرَّف في سبعة مواضع، و`Record<string,…>` الذي محا فحص المفاتيح.
- **#260 · تنافذ القوائم الطويلة** — نُشر أخيرًا (كان مدموجًا بلا نشر منذ 31 أغسطس).
- **رفع الملفات (#262، ينتظر الدمج)** — العيب: سياسة القراءة تُطبَّق على السطر الذي يعيده `INSERT … RETURNING`، وهي تبحث عنه في الجدول نفسه قبل أن يدخل اللقطة. **صفر رفعة ناجحة منذ 23 أغسطس.** ومعه: غياب `GRANT` من كل المهاجرات، ورسالة خطأ تعرض مفتاح ترجمة، وتثبيت `browserslist` فوق تحذيرين عاليين.


> المهام المكتملة تُنقل إلى هنا (لا تُحذف). الأحدث في الأعلى.

## 2026-08-31 — استيراد سجلّ المبيعات، والشاشة الجدارية، وأداء القوائم

**الاستيراد نُفِّذ ومُتحقَّق منه على الإنتاج:** 690 فرصة بصفر تكرار · 45 مشروعًا مرجعيًّا · 50 شركة · 132 جهة اتصال · 5 أهداف (2026 = 25,000,000). سجلّات الموظفين لم تُمسّ.

**والتنظيف تمّ:** 32 نسخة مكرّرة من جهات الاتصال (خطأ منّي — حرس `NOT EXISTS` كان يفحص الجدول لا الدفعة، وهو الفخّ نفسه الذي عالجته للمشاريع المرجعية ولم أعمّمه) و28 جهة اتصال و18 شركة من استيراد مارس السابق. وستّ شركات **أُعيد وسمها بدل حذفها** لأن 12 جهة اتصال من اليوم تشير إليها.

**وأربعة PRs دُمجت:** #256 (`extra_data`) · #257 (اللوحة) · #259 (قراءة الأهداف) · #260 (التنافذ).


## 2026-08-30 — اختبارات فحص نصّ المصدر: فرز وتصحيح، لا تحويل شامل
> مسحوب من `backlog.md` في 2026-08-25 · **اكتمل** · نُقل من `current.md` في 2026-08-30.

- **الفرز اكتمل: 55 ملفًا، 1051 تأكيدًا، كلها مصنَّفة ومقروءة.** معايير القبول الأربعة كلها محقَّقة.
- 🔴 **التأطير الأصلي للبند كان خاطئًا، والقياس هو ما كشفه.** كُتب بصيغة «403 تأكيدًا تفحص النص لا السلوك، حوّلها». الفرز الفعلي قال: 294 حارس غياب (أقوى استخدام لفحص النص، ولا مكافئ سلوكي له) · 666 توصيلًا (ضعيفة لا كاذبة) · 36 تأكيد SQL (لا Postgres في اختبار bun) · و**55 ادّعاء منطق — الفئة الوحيدة القادرة على الكذب**.
- **الخطر الحقيقي كان الإفراط في التحويل لا التقصير فيه:** تنفيذ البند كما كُتب أصلًا كان سيُفقد المشروع **996 تأكيدًا يحمل تغطية حقيقية** مقابل مظهر أنظف.
- **ما حُوِّل فعلًا** (بعد قراءة الـ55 واحدًا واحدًا): معاملات تبويبات `/quotations` — استُخرجت إلى `src/lib/pipeline-tabs.ts` مع 5 اختبارات سلوكية، وهي **نفس نمط خطأ الـdrilldown بالضبط** · وفجوتان حقيقيتان في سلسلة BAFO (مسح شامل لـ`ALL_ROLES` لكل خطوة يلتقط دورًا جديدًا يكتسب صلاحية صامتًا، وثابت D12).
- **قاعدة الطفرة طُبِّقت لا أُعلنت:** طفرة أبقت كل السلاسل في المصدر (`"boq"` يظهر 3 مرات) وغيّرت القيمة المُعادة — الـgrep راضٍ، وأربعة اختبارات سلوكية تفشل.
- **درسان من الفرز نفسه:** تقديري الأول (403) كان خطأً، وتقديري الثاني عن «فجوة BAFO» كان خطأً أيضًا — الملف كان يحوي الاختبارات السلوكية أصلًا، وبحثي استثنى ملفات `contract` وهي بالضبط حيث تسكن. **كلاهما انكشف بالقياس لا بالمراجعة.**
- **أداة خرجت من هذا البند:** `src/lib/source-under-test.ts` (`stripComments`/`readSource`) — أُنشئت بعد سقوط ثلاثة اختبارات تقرأ المصدر على **تعليقات تقتبس الكود الذي تمنعه**. من يفحص نصّ مصدر لاحقًا يبدأ منها.


## 2026-08-30 — لوحة القيادة: الإجمالي وتركيبه بدل خمس عشرة غيابًا
- الناتج: `PipelineComposition` (شريط تركيب + مفتاح، تدرّج `--stage-1..7` يشفّر حدّ «الملتزَم به») و`KpiGroup` (طيّ المؤشرات بلا رقم مع تسميتها وتجميعها بالسبب). توحيد تلوين رسم المراحل.
- القياس الذي بدأ منه العمل: 19 بطاقة فوق الطيّة، **15 فارغة**، الإجمالي مكرّرًا حرفيًا مرّتين، والرسوم السبعة الموجودة كلها تحتها.
- PR: #250 (يحمل #249 معه — أُنشئ فرعه من فرع #249 لا من `main`) — مدموج `31b0fa3`.
- **يمسّ البند النشط في `current.md`**: `src/lib/source-under-test.ts` أُنشئ اليوم بعد أن سقطت **ثلاثة** اختبارات تقرأ المصدر على تعليقات لا على كود. هذه فئة العيب التي يبحث عنها ذلك البند، وقد صار لها الآن أداة مشتركة — من يستأنفه يبدأ منها.
- **منشور ومُتحقَّق منه حيًّا** (2026-08-30): `--stage-1` و`--stage-7` موجودان في الـCSS المنشور على `agent.phc-sa.com`.

## 2026-08-25 — تدقيق شامل + إصلاح عنقود الـdrilldown (غير مُلتزَم على `main`)
- **البوابات كانت خضراء أصلًا** (1694 اختبارًا) — والسبب أن **55 من 98 ملف اختبار، و403 تأكيدًا، تفحص نص المصدر لا السلوك**. `expect(s).toContain("routeSearch.owner")` كان يمرّ بينما السلسلة داخل `useMemo` لا يذكرها في اعتمادياته. هذا الاستنتاج أهم من العيوب نفسها.
- 🔴 **قائمة الفرص تتجاهل المندوب والفترة من رابط اللوحة** — فتح مندوب ثم آخر بنفس المرحلة يعرض صفقات الأول تحت رابط الثاني.
- 🔴 **«مسح الفلاتر» لا يمسح المندوب والفترة** — قائمة فارغة بلا مخرج.
- **`describeFilters`/`hasActiveFilters` مستورَدتان ولم تُستدعَيا قط** — `no-unused-vars: "off"` أخفى ذلك. رُكِّب الشريط الذي كُتبتا له.
- **`/pipeline-overview` مسار لم يوجد قط** — حيّ ومكسور منذ PR #68. الوحيد من الخمسة الذي وصل الإنتاج فعلًا.
- **`inferredCount` يُحسب ولا يُعرض** رغم تعليق يَعِد بإظهاره.
- **الأسباب الجذرية عولجت:** `LinkProps["to"]` بدل `string` + `as any` (القائمة الجانبية و`StageDashLink`) — **تحقّقت أن الحارس يرفض المسار المكسور فعلًا (TS2322)**؛ و`SearchSchemaInput` يفصل نوع الإدخال عن المخرَج.
- **تراجعت عن تنظيف الكود الميت آليًا** — كسر ملفين (حذف `t` من عبارة تُفكِّك `lang`). عبارات التفكيك تخلط المستخدَم بغيره؛ نُقل إلى backlog.
- **لم أدمج `applySearch` مع مُرشِّح الصفحة عمدًا** — دلالتاهما تختلفان في الصفوف بلا تاريخ، والدمج كان سيغيّر السلوك.
- `bun run verify` أخضر: **1707/1707** · 0 أخطاء lint · build. صفر migrations، لا نشر.
- **متبقٍّ:** التغييرات في working tree على `main` — تحتاج فرعًا والتزامًا.

## 2026-08-21 — Security hotfix: عزل الوصول إلى المرفقات (PR #196 — مدموج ومنشور)
- 🔴 **كل مستخدم مسجَّل كان يقرأ كل مرفق** — السياسة كانت `bucket_id = 'attachments'` وحدها. استُبدلت بـ owner / role / linked-entity، والمسار غير المفهوم يفشل مغلقًا. `system_admin` و`viewer` مستثنيان عمدًا ([[D24]]).
- 🔴 **الرفع كان يخزّن رابطًا عمره 7 أيام** بدل المسار في 3 من 4 مواضع — الملف يموت بصمت بعد أسبوع. يُخزَّن المسار ويُوقَّع عند الضغط ([[D25]]).
- **خمسة مواضع قراءة** كانت تضع العمود في `href` مباشرة — بدون إصلاحها كانت الأدلة الجديدة ستظهر بلا رابط. تمرّ الآن عبر `AttachmentLink`.
- `upsert: false` (كانت `true`، تمسح الملف المتصادم بصمت) · TTL من 7 أيام إلى 10 دقائق.
- استرجاع حتمي فقط: 2 مسترجَع · 3 مُبلَّغ عنها · 1 كائن يتيم مُبلَّغ عنه لا محذوف. الأعمدة القديمة لم تُمسّ.
- migrations 109/110 مطبَّقة على الإنتاج · local = remote = 110، diff فارغ · نُشر على `ad41ccf`.
- 1273 اختبارًا · RLS مُثبت حيًّا (8 حالات) · اختبار رفع معزول مع تنظيف كامل (leftovers = 0) · idempotency ثلاث مرات ثابتة.
- **متبقٍ:** لا شيء في نطاق الإصلاح. القيد القائم: لا يوجد E2E عبر جلسة مسجَّلة الدخول (`playwright-smoke` = 2 passed / 105 skipped، محجوب بـMFA).

## 2026-08-18 (2) — Phase 3: JIH / Tender / Opportunity Execution (ينتظر المراجعة)
- **عطل حيّ:** `jih_bafo` و`contract_signed` غير قابلتين للوصول (الخادم يرفض ما يعرضه العميل). أُصلح + حارس يقارن الخريطتين ([[D19]]).
- `tender_subtype` · المقاول الفائز · رابط عكسي `source_tender_id` + منع التحويل المزدوج بفهرس فريد جزئي.
- `commercial_handoff_status` تسع حالات مستقلة عن المرحلة · فصل احتمالية الذكاء عن الإنسان ([[D20]]).
- 880 اختباراً (+59). migration محلية مختبَرة سلوكياً، **غير مطبَّقة remote**.
- **متبقٍ:** تطبيق الـmigration بموافقة · إعادة توليد `types.ts` (+ إعادة إلحاق الكتلة اليدوية) · فحص E2E لجانب المراجِع (محجوب بكلمات مرور fixtures قديمة).

## 2026-08-18 — Phase 2: Intake & Opportunity Review (ينتظر المراجعة)
- بوابة مراجعة قبل التسعير: أربعة قرارات، مدير المبيعات أو التطوير أيٌّ منهما وحده، `system_admin` وحده ممنوع — مفروض في القاعدة ([[D17]]).
- أربعة أنواع طلبات · حقول الاستقبال الناقصة · دورة «طلب معلومات» مع إعادة إرسال وعدّاد وسجل تدقيق.
- اكتمال الحزمة واقتراح النوع بقواعد حتمية ([[D18]]).
- 821 اختبارًا (+44). migration محلية مختبَرة سلوكيًا، **غير مطبَّقة remote**.
- **متبقٍ:** تطبيق الـmigration بموافقة · إعادة توليد `types.ts` بعدها · وكيل AI للاستخراج من الملفات (يحتاج نشر Edge Function).

## 2026-08-12 — Phase 1: Sales IA + canonical stage + admin governance (ينتظر المراجعة والفحص البصري)
- تنقّل Sales = أربع وجهات · مجموعة Home · خروج 5 مسارات من التنقّل مع بقائها عاملة · حذف `IntakeHubTabs`.
- `my-workspace` انتقل إلى `resolveCanonicalStage` (آخر سطح كان يقرّر من `stage` القديم). `stage`/`pipeline_step` deprecated لا محذوفين.
- **`system_admin` بلا سلطة تجارية** على طبقتي التطبيق والقاعدة ([[D15]]) — migration محلية additive مُختبَرة سلوكيًا، **لم تُطبَّق remote**.
- 777 اختبار (+46)، خمسة أُعيدت صياغتها من تثبيت التنفيذ إلى تثبيت السلوك.
- **متبقٍ:** فحص بصري للمتصفح (أداة المتصفح غير مستقرة، أربعة رموز MFA ضاعت) · مراجعة · دمج.

## 2026-08-10 — فحص QA حي وإصلاح ثمانية عيوب (ينتظر المراجعة)
- **الفحص:** 26 مسارًا محميًا + 6 عامة + 3 صفحات تفاصيل، بحساب `system_admin` على قاعدة الإنتاج، **قراءة فقط**. تسعة عيوب مرصودة، ثمانية حقيقية وواحد سُحب بعد التحقق.
- **الأخطر — البحث الشامل (⌘K) لا يعيد الفرص/المشاريع/جهات الاتصال:** الخادم يجد النتائج ثم تخفيها `cmdk` لأن قيمة كل عنصر كانت UUID، والفلتر المحلي يطابق النص المكتوب معها — فما احتوى حرفًا خارج hex لا يطابق. نفس الجذر منع رسالة "لا نتائج" فخرجت النافذة فارغة. أُطفئ الفلتر ونُقل المنطق إلى `src/lib/command-search.ts` وأُضيفت جهات الاتصال.
- **شاشة بيضاء على `/boq` و`/rfq-jih`:** `throw redirect` من `beforeLoad` تحت `_authenticated` (`ssr: false`) يترك شجرة Suspense غير محلولة. صار التحويل من المكوّن عبر `<Navigate>` كما في `src/routes/index.tsx`.
- **الباقي:** تعريب أخطاء المصادقة (4 شاشات) · ~60 مفتاح ترجمة لثلاث صفحات · **CSP مفروضة** بدل `Report-Only` بلا `report-uri` · نص فراغ خاص بسجل واحد · تنبيهات تتبع الاتجاه.
- **اختبار كان يحرس العيب:** `phase2-page-consolidation.contract.test.ts` يثبّت آلية `beforeLoad` المعطوبة؛ أُعيدت صياغته ليؤكّد السلوك. القرارات: **D13** (CSP) · **D14** (اختبار العقد يثبّت السلوك).
- **سُحب:** خطأ hydration (سمات `data-tsd-source` من إضافة التطوير؛ الإنتاج صفر أخطاء) · "التبويبات غير قابلة للحفظ كرابط" (خطأ قياس: `?tab=` موجود فعلًا).
- تحقّق: 731/731 اختبار (+36) · `typecheck` و`lint` و`build` خضراء · تحقّق حي بالمتصفح لكل إصلاح · **لا migrations ولا Edge Functions ولا نشر** · فرع `fix/qa-findings-2026-08-10` غير مدموج.

## 2026-08-05 (2) — تدفق الاستقبال: فرصة بدل مشروع، ثم نموذج واحد يوجّه نفسه
- **PR #171** (`27131bd`): `convertInboxToRfq` كانت تُنتج صف RFQ فقط بلا فرصة — فلا شيء يظهر في Pipeline بعد التحويل، والمرئي الوحيد هو المشروع الذي أجبرت النافذة على إنشائه. أصبحت تُنتج فرصة + RFQ مربوط + جهة اتصال + شركة + متابعة + نشاط، وتنتقل لصفحة الفرصة. **حُذف إنشاء المشروع من نافذة التحويل** — مكانه نهاية دورة الحياة (§29 + trigger `create_project_from_won_opportunity`). ضرر خفي كان: الـtrigger مشروط بـ`project_id IS NULL`، فالمشروع المبكر يمنع الصحيح للأبد.
- **PR #172** (`3b65ebd`): نموذج إدخال **واحد** (`NewIntakeDialog`) في ترويسة AppShell وصفحة الاستقبال بنفس المكوّن؛ يستنتج تصنيفه ويوجّه: `jih` ← فرصة، `tender` ← لوحة المناقصات بلا فرصة (§3/§27)، بلا نوع/اسم ← فرز يدوي. **حُذف `NewRfqDialog`** الذي بنيته في #169 — نموذجان على نفس الأرض أسوأ من واحد يوجّه نفسه.
- القرارات: **D10** (الفرصة عند التحويل، المشروع عند الفوز) · **D11** (نموذج واحد).
- تحقّق: 672/672 اختبار · **لا migrations ولا Edge Functions** · نشر Cloudflare تلقائي، SHA كل نشر يطابق دمجه · الإنتاج HTTP 200.

## 2026-08-05 — تدقيق حي على الإنتاج + إصلاح تدفق الاستقبال (ملاحظات فيصل الميدانية)
- **الفحص الحي:** استعلامات قراءة فقط على الإنتاج عبر Management API كشفت أن النظام مبني لكنه شبه فارغ (فرصة حقيقية واحدة من 4، صفر عقود/BOQ/ترسيات، 3 مستخدمين حقيقيين من 13). **قائمة عروض الأسعار الرئيسية لم تُنقل بعد.** لوحة الهدف تعرض صفرًا لغياب هدف سنوي وشهري للشهر الحالي معًا.
- **أُصلح:** `contract_signed` كانت تختفي من طابور الترسيات · `sales_stage` يبقى NULL في مسارَي إنشاء من 5 · لا تحقق من حدود التواريخ (سجل حي بسنة 275760) · **النموذج يُمسح بالكامل عند العودة للنافذة** (أصاب كل نوافذ النظام) · المشروع المُنشأ من نافذة التحويل يصل فارغًا · صفوف My Day جامدة · إعادة التصنيف بعد التحويل تُيتّم السجل.
- **أُضيف:** زر "+ طلب جديد" بالترويسة يُنشئ RFQ وفرصة معًا (§25/§6) · استنتاج التصنيف من النموذج (§25.3) · `updateInboxItem` (السجل كان write-once) · حقل `file_or_url` · `stage-canonical.ts` تحضيرًا لتوحيد حقل المرحلة (غير مستخدَم بعد).
- **موثَّق بلا تنفيذ:** خطة backfill لـ`sales_stage` · تدقيق idempotency يخلص إلى **عدم جدولة `run_automations` بحالته** (الـdedup بلا unique constraint، والشرط المستمر يُعاد رفعه — يحدث فعلًا بالإنتاج).
- القرارات: **D6** (`rfq_received` مرحلة الإنشاء) · **D7** (تصحيح الاستقبال قبل التحويل فقط) · **D8** (استنتاج التصنيف) · **D9** ("+ New RFQ" مسار موازٍ لا بديل).
- PR: #169 · مدموج (`488098c`) · `sales-os-api` v40→v41 · **صفر migrations** · Cloudflare Worker منشور · 662/662 اختبار.
- ملاحظة معلّقة: "Dependency audit" لا يزال فاشلًا على main (مسبق الوجود منذ #166، تبعيات dev tooling فقط).

## 2026-08-04 — "إنشاء جديد" مباشر لحقلي المشروع/الشركة بنافذة تحويل RFQ
- الناتج: عند تحويل عنصر مصنَّف RFQ بصندوق الاستقبال، حقلا Project/Company يدعمان الآن "+ إضافة جديد" مباشرة (نفس آلية `onCreateNew` الجاهزة والمستخدَمة أصلًا بمنتقي الشركة في Contacts).
- PR: #167 · مدموج (`51b7fe8`) · لا migrations · Cloudflare Worker منشور. تفاصيل كاملة في `docs/AI_HANDOFF.md`.

## 2026-08-04 — اختبار شامل بالمتصفح الحقيقي لكل النظام (/qa)
- الناتج: 4 مشاكل حقيقية أُصلحت — (1) `ai-orchestrator` كان معطَّلاً بالكامل بالتطوير المحلي (خلل استيراد `zod`، لا علاقة بالإنتاج)، (2) رسائل خطأ AI عامة غير مفيدة بسبب عدم استخدام `runAiAgent()` الموجودة أصلًا — رُحِّلت 9 نقاط استدعاء، (3) تحذير وصولية بكل `ActionDialog` بلا وصف، (4) تسمية "CRM" قديمة بصفحة Projects. فُحص واستُبعد: تحذير hydration (أداة تطوير TanStack فقط)، وخلل Kanban ظاهري (خطأ بسكربت الاختبار لا بالتطبيق).
- PR: #165 · مدموج (`49eda92`) · لا migrations · Cloudflare Worker منشور (لا تغيير بكود Edge Functions). تفاصيل كاملة في `docs/AI_HANDOFF.md`.

## 2026-08-04 — توسيع تغطية AI عبر النظام (فحص + تنفيذ)
- الناتج: إصلاح خللين حقيقيين مكتشَفين (Draft Follow-up، Scan Pipeline — كلاهما كان معطَّلاً منذ الإطلاق بسبب شكل طلب خاطئ)، 4 وكلاء AI جدد (`project_job_notes`، `project_budget_variance`، `commercial_risk_assessment` يغطي RFQ/Tender/Quotation/Accounts، `sales_report_insights`)، توحيد عرض KPIs في Agent Activity. كل صفحة رئيسية بالنظام لديها الآن نقطة تماس AI واحدة على الأقل (18 وكيل إجمالاً).
- PR: #163 · مدموج (`1f15bd8`) · لا migrations · Cloudflare Worker + Edge Functions (`ai-orchestrator` v27، `sales-os-api` v40) منشورة فعليًا. تفاصيل كاملة في `docs/AI_HANDOFF.md` وَ`docs/ai-orchestrator.md`.

## 2026-08-04 — حذف زر "طلب جديد" من Lead & Tender Inbox
- الناتج: صفحة الاستقبال أصبحت بمدخل إنشاء واحد ("إدخال جديد") بدل اثنين — JIH/Tender يبقيان متاحين بالكامل عبر إدخال جديد ← تصنيف ← تحويل. حذف `createJihRequestWithOpportunity` غير المستخدَم + مفاتيح i18n يتيمة.
- PR: #161 · مدموج (`71de821`) · لا migrations · Cloudflare Worker منشور. تفاصيل كاملة في `docs/AI_HANDOFF.md`.

## 2026-08-03/04 — لوحة مشاريع Production + نقلها لقسم الإنتاج
- الناتج: `project_number` تلقائي (مشاريع + استقبال)، صورة غلاف، Job Pipeline (Kanban مرن بـ`@dnd-kit`)، Budget، نقل قسم المشاريع من المبيعات إلى الإنتاج مع ربط تلقائي عبر trigger عند فوز الفرصة، Discussion قابل للتعديل/الحذف + منشن، Client Details قابل للتعديل، زر "New Opportunity" من صفحة Account، إصلاح خلل توجيه `/projects/$id` القديم.
- PR: #160 · مدموج (`078484b`) · 4 migrations مطبَّقة على الإنتاج (`lrfdtoexyeghrzynapyn`) · Cloudflare Worker منشور. تفاصيل كاملة في `docs/AI_HANDOFF.md`.
- ملاحظة معلّقة: فحص "Dependency audit" الأمني فشل بعد الدمج — مسبق الوجود وغير مرتبط (تبعيات dev tooling فقط)، يحتاج PR منفصل صغير.

## 2026-07-23 — Pathfinder D1–D6: توحيد التكرار المعماري
- الناتج: 6 حالات تكرار/مخاطر معمارية (من مسح Pathfinder 2026-07-22 + اكتشاف D6 عبر `/investigate`) نُفِّذت كل واحدة في worktree منفصل عبر subagent-driven-development، رُوجعت مرتين (بناء + fresh-eyes مستقلة)، وكل الفجوات المطروحة (اختبار انحدار D4، فحص متصفح يدوي D3/D6، live smoke test لـ D2/D5) أُغلقت، ثم دُمجت جميعها إلى main.
- PRs (بترتيب الدمج): #116 (D1 — توحيد company-name normalization) · #117 (D3 — نقل زر Scan Pipeline) · #118 (D4 — حذف `/team` غير المحمي) · #119 (D2 — helper مشترك لإنشاء leads) · #120 (D5 — توحيد حذف import batches تحت التدفق المحكوم) · #121 (D6 — توحيد حساب KPIs بين اللوحات).
- ملاحظة معلّقة: migration D5 (`20260723120000_extend_delete_allowlist_import_batches.sql`) على main لكن لم تُنشر إلى الإنتاج بعد — تحتاج بوابة الموافقة المنفصلة.

## 2026-07-20 — Data Import → Live CRM (Part 3/3)
- الناتج: التزام المرشّحين المعتمَدين إلى الـ CRM الحي.
- PR: #108 · commit `4c1ffe4`.

## 2026-09-07 — Activated opportunity display
PR 288 merged and deployed (04e706c); readiness 34126413617 passed. Historical classification fallback and active assignment choices completed.

## 2026-09-07 — All-data production reconciliation verified

PR 289 is merged and deployed: `7d1b9c4cfa24ac39cccfed70ac2fea87940de9af`,
Worker `34d4f0af-eae3-4ff4-b0fa-554926251d8b` at 100%, sales-os-api v49,
migration `20260928150000`. Production run 34128900769 and post-production
readiness 34129067951 passed; canary/readiness 34128152747/34128332893 passed.
Preview URLs were disabled again. A fresh public-data backup preceded execution.

The actual signed-in commercial user reconciled 125 promoted opportunities and
archived 126 exact legacy copies. Production verifies 126 receipts by the actual
user, all before-images/legacy notes preserved, zero remaining matches between
these promotions and live legacy copies, zero open legacy flags or independent
references, and unchanged owners, companies, stages, values and quotes for all 125.
821 opportunity rows remain preserved: 695 non-archived and 126 archived. Archive
source count 679 and digest 29e67ec47426bf7676112331b156ec80 are unchanged.
The 80 new promotion records were NOT 80 net-new business deals: they overlapped
legacy imports, as did 45 earlier promotions. The correction removes SAR 214,623,668
of duplicated legacy values without changing canonical amounts.

All 679 source rows were checked:634 have exactly one live CRM match, 43 cannot be
linked automatically under complete-identity rules, and 2 map to the same unresolved
legacy pair (AH23162/ALECFITOUT, no owner/company mapping). This pair is flagged in
open duplicate group 26d86997-a66b-4fff-b8da-51a923d069d7 for commercial review.
Do not invent owners, companies, values or reopen won/lost work. The remaining 554
without historical quotation promotion are not necessarily absent from CRM.

UI verified completion and repeat-preview idempotency, Fairmont legacy follow-up
notes and both-direction record links, JIH display and active-only assignment choices.
Validation: 2674 Bun, 17 Deno, 971 full local DB checks + 2 additional behavior cases; all required
CI/security/database/role checks passed. No further application deployment is required
for this documentation-only update.
