# Current Task — PHC Command Center

> بند واحد فعّال. عند اكتماله انقله إلى `completed.md` واسحب التالي من `backlog.md`.

## Objective
**اختبارات فحص نصّ المصدر — فرز وتصحيح، لا تحويل شامل.**
مسحوب من `backlog.md` في 2026-08-25.

## ⚠️ تصحيح للتأطير الأصلي — اقرأه قبل أي عمل

البند كُتب أصلًا بصيغة «403 تأكيدًا تفحص النص لا السلوك، حوّلها». **القياس الفعلي
يقول غير ذلك، والصيغة الأصلية كانت مبالغة تُنتج عملًا خاطئًا:**

| الفئة | العدد | الحكم |
|---|---|---|
| تأكيدات على ملفات SQL | 387 | **مشروعة** — لا يمكن تشغيل Postgres في اختبار bun، والسلوك الحقيقي مُغطّى في `tests/db-behaviour/` |
| حرّاس غياب (`not.toContain`) | 77 | **من أقوى الاستخدامات** — «الكود يجب ألّا يفعل X» خاصية نصّية بطبيعتها ولا تُختبَر سلوكيًا بسهولة |
| توصيل/معرّفات | ~284 | ضعيفة لكنها ليست كاذبة — **إلا حين تُقرأ كضمان سلوكي** |
| فحص تعليقات | 11 (فُحصت يدويًا: 2 عيب حقيقي) | معظمها حرّاس توثيق مقصودة واسم الاختبار يقولها |

**العيب الحقيقي فئة ضيّقة:** *فحص نصّي يقوم مقام سلوكٍ قابل للاختبار وغير مُختبَر.*
هذا بالضبط ما حدث في خطأ الـdrilldown: `toContain("routeSearch.owner")` قام مقام
«القائمة تحترم المندوب»، وكان قابلًا للاختبار ولم يُختبَر.

## معيار الفرز (طبّقه على كل تأكيد)

1. **يبقى** إن كان `not.toContain` — حارس سياسة/انحدار.
2. **يبقى** إن كان على SQL migration — مع تفضيل نقله إلى `tests/db-behaviour/` عند توفّر الجهد.
3. **يبقى** إن كان حارس توثيق **واسم الاختبار يعلن ذلك صراحةً**.
4. **يبقى** إن أثبت توصيلًا — لكن **افحص العلاقة لا النثر عنها**
   (`toContain('from "@/lib/stage-canonical"')` لا `toContain("stage-canonical.ts")` داخل تعليق).
5. **يُحوَّل** إن قام مقام سلوك يمكن استدعاؤه مباشرة.
6. **قاعدة الطفرة — الفيصل:** اكسر الكود عمدًا. إن بقي الاختبار أخضر فهو ليس اختبارًا.

## نتيجة الفرز الكامل (2026-08-25) — 55 ملفًا، 1051 تأكيدًا

| الفئة | العدد | القرار |
|---|---:|---|
| حرّاس غياب (`not.toContain`) | 294 | **تبقى** — أقوى استخدام لفحص النص، ولا مكافئ سلوكي لها |
| توصيل/معرّفات | 666 | **تبقى** — تُثبت أن الملف يشير إلى الرمز؛ ضعيفة لكنها ليست كاذبة |
| تأكيدات SQL | 36 | **تبقى** — لا Postgres في اختبار bun |
| **ادّعاءات منطق** | **55** | **الفئة الوحيدة القادرة على الكذب** — تُفحص واحدة واحدة |

الـ55 موزّعة على 23 ملفًا. **الفرز لا يعني التحويل:** بعد القراءة، معظمها مشروع.

### ما فُحص وبقي (بالمعيار)
- `security-baseline` — يؤكّد `existsSync(team.tsx) === false`: حقيقة غير قابلة للتحايل، مقرونة بفحص حارس `admin-settings`. **مصمَّم جيدًا.**
- `query-performance` — حارس توثيق واسم الاختبار يعلنه: *"explains why, so the next person does not tidy it away"*.
- `historical-promotion-path` — يفحص نصّ رسالة `RAISE` في SQL: عقد سلوكي حقيقي.
- `phase-4-notifications` (3) — إيجابيات كاذبة من الكاشف: `||` هنا وصل نصوص SQL لا «أو» منطقية.
- `bafo-approval-chain-frontend` (7) — **ظننته فجوة وكنت مخطئًا**: الملف يحتوي أصلًا اختبارات سلوكية تستدعي الدوال وتثبّت ثوابت `system_admin`. بحثي الأول استثنى ملفات `contract` وهي بالضبط حيث تسكن.

### ما حُوِّل فعلًا
- **معاملات تبويبات `/quotations`** — كانت `validateSearch` تحليلًا داخليًا يحرسه `toContain('s.tab === "boq"')`. استُخرجت إلى `src/lib/pipeline-tabs.ts` مع 5 اختبارات سلوكية. **نفس نمط خطأ الـdrilldown بالضبط.**
- **فجوتان حقيقيتان في سلسلة BAFO** — مسح شامل لـ`ALL_ROLES` لكل خطوة (يلتقط دورًا جديدًا يكتسب صلاحية صامتًا)، وثابت D12 (شخص واحد يحمل التقدير والمالية عمدًا).

### قاعدة الطفرة — مُطبَّقة لا مُعلَنة
طفرة أبقت كل السلاسل في المصدر (`"boq"` يظهر 3 مرات) وغيّرت القيمة المُعادة: الـgrep راضٍ، وأربعة اختبارات سلوكية تفشل. وطفرة تُعيد تجاوز `system_admin` على خطوة المالية: التقطتها التغطية القائمة والجديدة معًا.

### درسان من الفرز نفسه
1. **تقديري الأول (403) كان خطأً، وتقديري الثاني عن فجوة BAFO كان خطأً أيضًا.** كلاهما انكشف بالقياس لا بالمراجعة.
2. **الخطر الحقيقي في هذا البند هو الإفراط في التحويل.** لو نُفِّذ كما كُتب أصلًا لفقد المشروع 996 تأكيدًا يحمل تغطية حقيقية مقابل مظهر أنظف.

## Files
- `src/lib/phase-1-sales-ia.contract.test.ts` ✅ (حالتان حُوِّلتا)
- `src/lib/phase-5-ui-wiring.contract.test.ts` ✅ (حُوِّل ضمن PR #215)
- `src/lib/pipeline-tabs.ts` + `.test.ts` ✅ (جديد — استُخرج من `quotations.tsx`)
- `src/lib/roles.capabilities.test.ts` ✅ (فجوتا BAFO)
- **الفرز اكتمل. لا ملفات متبقية.**

## Acceptance Criteria
- [x] قياس فعلي بدل التقدير (1112 تأكيدًا، مُصنَّفة)
- [x] معيار فرز مكتوب يمنع التحويل الأعمى
- [x] تحويل الحالات المُثبَتة، وكل تحويل مُتحقَّق منه بطفرة
- [x] تمرير كل الملفات بالمعيار — 55 ملفًا، 1051 تأكيدًا، مُصنَّفة ومقروءة

## Risks
- **الخطر الأكبر هو الإفراط في التحويل:** حذف حارس غياب أو حارس SQL يفقد تغطية حقيقية
  مقابل «نقاء» شكلي. المعيار أعلاه موجود لمنع ذلك تحديدًا.
- بيانات CRM حية — أي التزام/نشر يمرّ ببوابة موافقة.

## Done Definition
`bun run verify` ينجح · PR مفتوح · docs/AI_HANDOFF.md محدّث.
