# Current Task — PHC Command Center

> بند واحد فعّال. عند اكتماله انقله إلى `completed.md` واسحب التالي من `backlog.md`.

## بند نشط — إنهاء الشاشة الجدارية وتشغيلها

### ١ · ما ينتظرك أنت
- **أنشئ `info@phc-sa.com`** (Supabase Dashboard ← Authentication ← Add user)، فعّله، وأسند له **`bd_manager`** من `/admin-settings`. لا أنشئ حسابات ولا أُدخل كلمات مرور.
- **انشر `main` @ `bc38e02`** — #260 (تنافذ القوائم) مدموج ولم يصل الإنتاج بعد. المنشور حاليًّا `bb5d6f6`.

  ```
  gh workflow run deploy-cloudflare.yml --ref main \
    -f target=production -f confirm=agent.phc-sa.com
  ```

  **المدخلان إلزاميان.** بدونهما يأخذ التدفّق الافتراضي `canary`، فيمرّ **أخضر بالكامل** بينما `Deploy production custom domain` تُتخطّى والإنتاج بلا تغيير. وقع هذا اليوم مرّة.

- **اقفل جهاز العرض** — kiosk في Chrome أو Guided Access. اللوحة صفحة بلا تنقّل، **لا حاجزٌ أمني**، و`bd_manager` يقرأ كل بيانات المبيعات لمن يكتب عنوانًا آخر.

### ٢ · اللوحة الجدارية — في PR ينتظر مراجعتك
`feat/board-rebuild` — إعادة بناء كاملة على مراجعة المنتج: حالات فارغة صادقة، ودلالة لونية، ورقاقات محاذاة، وشريط أخبار متحرّك، وثمانية عيوب صامتة كشفها القياس.

**2459 اختبارًا خضراء · صفر خطأ typecheck · البناء ينجح.**

**تحذير تشغيليّ:** أوقف خادم المعاينة قبل أي سكربت يعيد كتابة `board.tsx` — مولّد المسارات في TanStack أتلف الملف مرّة، فاختصر 850 سطرًا إلى سقالة من 9.

### ٢ب · النشر بعد الدمج لا قبله
`#260` (تنافذ القوائم) مدموج ولم يُنشر، والمنشور `bb5d6f6`. **لا تنشره وحده** — انتظر دمج لوحة العرض فيغطّيهما نشرٌ واحد بدل اثنين.

### ٣ · قرارات تخصّك، لا كودًا
- **الهدف السنوي كلّه على حساب واحد** (`sales_targets` مفتاحه المستخدم، ولا صفّ لهدف شركة). فيقرأ الجدول كأن عمر فشل وعبدالرحمن يعمل بلا هدف. إمّا توزيعه، أو صفّ فريق مجمَّع.
- **`source_registry` فارغ تمامًا** — فالفرص الأربع المحتملة محجوبة بـ`lead_guard`. اعتماد `protenders` يكتبه `sales_manager` أو `ceo`.
- **`VENDOR PORTALS`** فيها كلمات مرور نصّية في مصنّف يُشارَك برابط.
- **الملف المستورَد لقطة لا رابط حيّ.** إن استمرّ الفريق على SharePoint فلن يصل شيء. جسر القراءة في **PR #258 مسوّدة** — ولا أنصح به إن كان الانتقال شهرًا: الرفع اليدوي عبر صفحة الاستيراد يكفي بكلفة إعداد صفر.

### ٤ · حقول فارغة تحجب نصف اللوحة
```
human_win_probability   0 / 739      → لا توقع مرجّح ولا تغطية
next_action             0 / 739      → لا «بلا إجراء قادم»
next_action_due         0 / 739      → لا 30/60/90
last_activity_at       46 / 739      → «متوقّفة» تعمل على 6% فقط
```
اللوحة تعلن هذه الحالات بدل طباعة `SAR 0`. **امتلاؤها قرار تشغيلي يفتح نصف المراجعة.**
