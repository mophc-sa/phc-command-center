# Current Task — PHC Command Center

> بند واحد فعّال. عند اكتماله انقله إلى `completed.md` واسحب التالي من `backlog.md`.

## بند نشط — اعتماد استيراد 2026-08-31

**جاهز وينتظر تنفيذك، لا عملًا مني.** سكربتان في scratchpad الجلسة، كلاهما يبدأ بـ`BEGIN`
وينتهي بـ`COMMIT` معلَّق:

1. **اعتمد المهاجرة** `20260918100000_reference_projects_extra_data.sql` عبر مسار المهاجرات
   المعتاد (السكربت يحمل `ADD COLUMN IF NOT EXISTS` احترازيًّا فلا يسقط إن سبقته).
2. **`phc-import.sql`** — 690 فرصة + 5 أهداف سنوية. راجع الأعداد قبل `COMMIT`.
3. **`phc-import-2.sql`** — 65 شركة · 133 جهة اتصال · 45 مشروعًا مرجعيًّا · 4 فرص محتملة.

**ما يستحق مراجعة بشرية بعد الاعتماد:** الـ239 صفًّا التي وُزّع مالكها حتميًّا على فيصل
وعبدالرحمن لأن كودها بلا بادئة معروفة. كل واحد منها يحمل
`extra_data->>'owner_assignment' = 'distributed'` وكوده الأصلي، فتصحيحها استعلام واحد.

**وقرار منفصل عاجل:** ورقة `VENDOR PORTALS` في المصنّف المشترك فيها كلمات مرور نصّية.

## لا بند نشط (سابقًا)

البند السابق — **«اختبارات فحص نصّ المصدر: فرز وتصحيح»** — اكتمل ونُقل إلى
[`completed.md`](completed.md) في 2026-08-30. الفرز غطّى 55 ملفًا و1051 تأكيدًا،
ولا ملفات متبقية.

**لم أسحب البند التالي عمدًا.** السحب من `backlog.md` يعني بدء العمل، وذلك قرار
نطاق يخصّك أنت — والقاعدة في `CLAUDE.md` صريحة: *لا تبدأ تنفيذ أي بند من
`backlog.md` قبل نقله إلى `current.md`*.

## أقوى المرشّحين — مرتَّبين بما يفتحه كلٌّ منهم

**١ · تحقّق من أرقام اللوحة ببياناتك الحقيقية** *(ليس في `backlog.md` — مفتوح من جلسة اليوم)*
البند الوحيد الذي لا يستطيع أي مساعد إغلاقه. شريط التركيب، ودونات جاهزية التوقّع،
ونسبة تحقيق الأهداف (التي ارتفعت بعد #251) — كلها رُئيت من مقعد معاينة ببيانات
مقدَّرة من لقطة شاشة، **لا من الإنتاج**. رقم يبدو صحيحًا وليس كذلك أسوأ من رقم
يعترف بعجزه.

**٢ · قرارا عمل يفتحان خمسة مؤشرات ولا يحتاجان كودًا**
ضبط هدف مبيعات سنوي (وحده يُخرج **خمسة** مؤشرات من الطيّة إلى بطاقات)، وتعبئة
`sla_policies` (فرع `stalled_deal` صامت بالتصميم بلا حدّ معتمَد).

**٣ · `.github/workflows/dependabot-lockfile.yml`** — 🔴 الأعلى خطرًا في `backlog.md`
الترقيع الأمني التلقائي معطَّل منذ 26 يوليو؛ كل PR تبعيات محجوب. الآلية مُثبَتة
عمليًا، لكن **قرارًا مطلوبًا**: الدفع بـ`GITHUB_TOKEN` لا يُعيد تشغيل CI — إمّا
إغلاق/إعادة فتح الـPR يدويًا، أو إضافة PAT كـsecret.

**٤ · `my-workspace` لم يُمسّ** — 23 صندوقًا، نفس مرض مركز القيادة قبل #250.
`KpiGroup` و`PipelineComposition` جاهزان لإعادة الاستخدام هناك، فالتكلفة أقل مما كانت.

**٥ · ~62 تعريفًا ميتًا** — منها استعلاما شبكة حقيقيان في `my-workspace.tsx`
(`ws-recs` و`ws-rfqs`) **يُنفَّذان ولا تُعرض نتائجهما**: رحلتان مهدرتان لكل فتح صفحة
لكل دور غير المندوب. ⚠️ **الحذف الآلي كسر ملفين من قبل** (أزال `t` من عبارة تُفكِّك
`lang` المستخدَم) — عبارات التفكيك تخلط المستخدَم بغيره، فالمراجعة يدوية.

## Risks (تنطبق على أي بند يُسحب)
- بيانات CRM حية — أي التزام/نشر يمرّ ببوابة موافقة. راجع
  [`docs/deployment-governance.md`](../docs/deployment-governance.md).
- أي PR يغيّر تدفق المستخدم **يُحدِّث [`docs/USER_GUIDE.md`](../docs/USER_GUIDE.md)
  في نفس الـPR** — لا كدفعة لاحقة.

## Done Definition
`bun run verify` ينجح · PR مفتوح · [`docs/AI_HANDOFF.md`](../docs/AI_HANDOFF.md) محدّث.
