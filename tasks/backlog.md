# Backlog — PHC Command Center

> أفكار/مهام غير مجدولة. **لا يبدأ الـ AI تنفيذ أي بند** إلا بعد نقله إلى `current.md`.

- [~] 🔬 **(اكتُشف 2026-08-25 — مسحوب إلى `current.md` في نفس اليوم)** اختبارات فحص نصّ المصدر. **⚠️ تأطيري الأصلي («403 تأكيدًا تفحص النص لا السلوك، حوّلها») كان مبالغة، والقياس الفعلي صحّحه:** العدد 1112 لا 403؛ منها 387 على SQL (مشروعة — لا Postgres في اختبار bun) و77 حارس غياب (**من أقوى الاستخدامات** لا أضعفها). العيب الحقيقي فئة ضيّقة: *فحص نصّي يقوم مقام سلوك قابل للاختبار وغير مُختبَر* — وهو ما حدث في خطأ الـdrilldown. معيار الفرز الكامل وقاعدة الطفرة في `tasks/current.md`. **لا تُحوَّل جملةً — الإفراط في التحويل يفقد تغطية حقيقية.**
- [ ] 🧹 **(اكتُشف 2026-08-25)** **~62 تعريفًا ميتًا** يكشفها `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`. منها استعلاما شبكة حقيقيان في `my-workspace.tsx` (`ws-recs` و`ws-rfqs`) **تُنفَّذان ولا تُعرض نتائجهما** — رحلتان مهدرتان لكل فتح صفحة لكل دور غير المندوب. **تحذير من التجربة:** الحذف الآلي كسر ملفين (أزال `t` من عبارة تُفكِّك `lang` المستخدَم) — عبارات التفكيك تخلط المستخدَم بغيره، فالمراجعة يدوية.
- [ ] **(اكتُشف 2026-08-25)** `@typescript-eslint/no-unused-vars: "off"` في `eslint.config.js` هو ما يُبقي التوصيلات الميتة غير مرئية (`describeFilters`/`hasActiveFilters` كانتا مستورَدتين وغير مستخدَمتين). تفعيلها قرار منفصل — سيُظهر البند أعلاه كأخطاء lint دفعةً واحدة.
- [ ] **(اكتُشف 2026-08-25)** `applySearch` في `drilldown.ts` **كود ميت إنتاجيًا** — تستخدمه اختباراته فقط. لا يُدمَج مع `matchesOpportunitySearch` بلا قرار: دلالتاهما تختلفان عمدًا في الصفوف بلا تاريخ.
- [ ] **(اكتُشف 2026-08-25)** `error-ingest` نقطة عامة (بلا JWT) تكتب `browser` و`extra` كـJSON **بلا سقف حجم** — بقية الحقول مقصوصة بـ`.slice()`. تحديد المعدّل لكل IP أفضل-جهد فقط (Map على مستوى الوحدة، يُصفَّر عند cold start).

- [ ] (غير عاجل — مؤجَّل بقرار 2026-07-23) تنفيذ الفحوصات الوظيفية + UAT على الإنتاج لـ D1/D2/D5 (بعد نشر migration D5 وEdge Functions المتأثرة — القسمان 3 و6 من قائمة التحقق المحفوظة في Obsidian `1. PROJECTS/PHC/PHC Command Center — Post-Deploy Checklist (Jul 23 2026).md`، تحتاج موافقة صريحة لكتابة بيانات اختبار في CRM حي). **لم تُنفَّذ اليوم (2026-07-26) لنفس السبب — تحتاج موافقة صريحة منفصلة.**
- [x] ~~نشر migration `20260726100000_document_leads_source_owner_id.sql` إلى الإنتاج~~ — نُشرت 2026-07-26 ضمن دفعة الـ4 migrations المعلَّقة (راجع تقرير النشر في docs/AI_HANDOFF.md).
- [x] ~~postcss high CVE (GHSA-6g55-p6wh-862q, GHSA-r28c-9q8g-f849)~~ — أُصلح 2026-07-26 عبر `overrides: { "postcss": "^8.5.18" }` (يحل لـ 8.5.23). `bun run verify` نظيف. لا كسر — postcss لم يغيّر شكل الـ API.
- [x] ~~brace-expansion high CVE (GHSA-mh99-v99m-4gvg / CVE-2026-14257)~~ — أُصلح 2026-07-27 عبر ترقية `eslint` إلى `^10.8.0` (يسحب `brace-expansion@5.0.8` المُرقَّع فعليًا، تحقَّقنا عبر `bun pm ls --all`). `eslint-plugin-react-hooks`'s قواعد "React Compiler readiness" الجديدة أُبقيت مُعطَّلة عمدًا (نطاق منفصل — راجع `docs/KNOWN_ISSUES.md`). `bun run verify` نظيف بالكامل.
- [ ] مراجعة إعداد GitHub Required status checks: فحص "Dependency audit" أحمر على main حاليًا ولم يمنع دمج PRs — تأكيد إن كان مُدرَجًا فعلًا ضمن الفحوصات المطلوبة (متوقَّع أن يكون، حسب `docs/security/security-baseline.md`).
- [x] ~~PR #75/#76: `bun.lock` الذي أنشأه dependabot معطوب~~ — **التشخيص كان خاطئًا (صُحِّح 2026-08-25).** dependabot **لا يُنشئ `bun.lock` إطلاقًا**؛ تحقَّقنا من ملفات #75 و#135 و#183: كلها تُغيّر `package.json` وحده. راجع `docs/KNOWN_ISSUES.md` للسبب الجذري والإصلاح المقترح.
- [ ] 🔴 **(2026-08-25) ادفع `.github/workflows/dependabot-lockfile.yml`** — بدونه يبقى الترقيع الأمني التلقائي معطَّلًا (كل PR تبعيات محجوب منذ 26 يوليو). الآلية مُثبَتة عمليًا. **قرار مطلوب:** الدفع بـ`GITHUB_TOKEN` لا يُعيد تشغيل CI — إمّا إغلاق/إعادة فتح الـPR يدويًا، أو إضافة PAT كـsecret.
- [ ] **(2026-08-25) #183 يحتاج ترحيلًا لا دمجًا** — 30 خطأ نوع في 5 ملفات من أربع قفزات كبرى (react-table 8→9 · recharts 2→3 · lucide 0→1 · react-day-picker 9→10). مُختبَر في worktree.
- [ ] **(2026-08-25) #135: خُذ الأربعة الآمنة وثبّت `typescript` على ^5.8.3** — TS 7 يُسقط `eslint` كليًا (`typescript-eslint` لا يدعمه، upstream #10940). بدون ترقية TS البوابات الأربع خضراء — مُتحقَّق منه.
- [ ] **(2026-08-25) #182 متأخر 102 commit** — يفشل في `Dependency audit` وحده لأن قفله يسبق overrides الأمان. دمج `main` فيه يكفي.
- [ ] **(2026-08-25) #209 (actions/cache) أخضر بالكامل** — جاهز للدمج، لا حاجز.
- [x] ~~مراجعة/دمج 5 PRs من dependabot المفتوحة~~ — دُمجت الثلاثة الآمنة (GitHub Actions: #72, #73, #74) بعد CI أخضر 2026-07-26. #75/#76 مؤجَّلتان (بند منفصل أعلاه، لقطة/lockfile معطوب).
- [x] ~~حسم مصير ملفات Docker غير المتتبَّعة~~ — لم تكن فجوة فعلية: كانت مُلتزَمة بالفعل منذ PR #113 (commit `98220e3`)؛ الملاحظة في ROADMAP.md كانت قديمة/غير دقيقة وصُحِّحت 2026-07-26.
- [x] ~~فتح issue لـ bug اختبارات ai-orchestrator*.contract.test.ts~~ — أُصلح مباشرة 2026-07-26 (لم يعد يحتاج issue منفصل): استبدال `readdirSync().find()` بمسار صريح لكل ملف migration في `ai-orchestrator.contract.test.ts` وai-orchestrator-hardening.contract.test.ts`.
- [x] ~~تأكيد منتج: توسيع مطابقة الأسماء العربية (D1)~~ — قُرِّر ونُفِّذ 2026-07-26: يشمل project_name وmain_contractor. انظر docs/DECISIONS.md.
- [x] ~~تأكيد منتج: leads.source/owner_id (D2)~~ — قُرِّر ووُثِّق 2026-07-26. انظر docs/DECISIONS.md.
- [x] ~~حذف ملفات .handoff/rbac-hardening-sprint8-*~~ — حُذفت 2026-07-26.
- [x] ~~افتح PR لفرع `fix/phase1-quick-ux-fixes`~~ — مُدمَج فعليًا كـ PR #127 (الملاحظة كانت قديمة/غير محدَّثة).
- [x] ~~Phase 2: دمج النماذج + توحيد صفحات الـ pipeline~~ — نُفِّذ 2026-07-26 (تنفيذ مباشر بموافقة المستخدم، بدون توقف للمراجعة). حوار New Entry موحَّد (Intake/Lead/RFQ/Quotation/BOQ) + إلغاء RFQ & JIH Board وBOQ Center كصفحتين (redirect لـ Quotations بتبويبات). Discovery/leads لم يُدمَج ضمن النموذج الموحَّد بعد — نوع "lead" في NewEntryDialog يغطي إنشاء lead جديد، لكن صفحة `/discovery` نفسها لم تُلغَ (بند متبقٍ إن أراد المستخدم إلغاءها أيضًا).
- [x] ~~Phase 3: لوحتا Sales وManagement منفصلتان~~ — نُفِّذ 2026-07-26. البنية التحتية (my-workspace.tsx الشخصية) كانت موجودة أصلًا؛ الفجوة الفعلية كانت التوجيه (salesperson كان يهبط على /command-center بدل /my-workspace) — أُصلح. أُضيفت بطاقة "Team Target" مجمَّعة في /command-center للإدارة. **لم يُستخدَم بعد**: `computeSalespersonMetrics`/`computeManagerMetrics` من `targets-metrics.ts` (تلك الدوال الأكثر تفصيلًا تبقى خاصة بصفحة `/targets` — لم يُدمَجا في لوحتي الهبوط، بند مستقبلي إن رغب المستخدم بمزيد من التفصيل هناك).
- [ ] **(محظور فعليًا)** بيانات: تعيين أهداف فعلية لـ Faisal وAbdelrahman (10M لكل منهم حسب طلب العميل) في جدول `sales_targets` — تحقَّقنا مباشرة من `public.profiles` في الإنتاج (2026-07-27): **لا حساب لأي منهما في النظام**. يحتاج إنشاء/دعوة الحسابين أولًا (قرار عمل خارج نطاق الكود) قبل إمكانية التنفيذ.
- [x] ~~Phase 4: Milestone checklist + Technical Notes~~ — نُفِّذ 2026-07-26. جدول جديد `opportunity_milestones` (7 عناصر ثابتة، مستقلة عن `sales_stage`، RLS بنفس نمط stakeholders/evidence_sources) + عمود `opportunities.technical_notes`. لوحتان جديدتان في صفحة الفرصة (checklist بجانب Evidence، Technical Notes في تبويب Assignment). لم يُدمَج مع `evidence_sources` عمدًا (نموذج بيانات مختلف تمامًا — سجل مستندات حر مقابل قائمة ثابتة).
- [x] ~~Phase 5: مراقبة BOQ/package متغيّر حسب كل مقاول~~ — نُفِّذ 2026-07-26. الاكتشاف: نموذج البيانات يدعم هذا أصلًا (`opportunities` لديها `project_id` وَ`main_contractor_id` منفصلان — عدّة فرص/مقاولين يمكن أن يتشاركوا نفس المشروع، كل واحدة بمرحلتها وحالة الباكج والـ BOQ الخاصة بها). الفجوة الفعلية كانت أن صفحة تفاصيل المشروع لا تُظهر ذلك — أُضيف: اسم المقاول + حالة الباكج + حالة BOQ لكل فرصة مرتبطة في لوحة "Opportunities"، مع رسالة توضيحية عند وجود أكثر من مقاول. تحقّق حي عبر PostgREST محليًا (مقاولان على نفس المشروع، كل واحد بحالة مختلفة) قبل الالتزام.
- [x] ~~Data Import: إصلاح فقدان البيانات غير المُطابَقة لعمود صمتًا~~ — نُفِّذ 2026-07-27. راجع `docs/KNOWN_ISSUES.md`.
- [ ] Data Import: ربط وكيلَي AI الجاهزين وغير المستخدَمين `data_cleanup` و`contact_mapping` بواجهة مستخدم فعلية (مبنيّان ومُختبَران، بلا أي زر/لوحة تستدعيهما حاليًا) — راجع `docs/ai-orchestrator.md` قسم "Later agents".
- [ ] Data Import: توحيد مخرجات `relationship_resolver` مع جدول `import_candidate_links` (مُخصَّص لذلك أصلًا) بدل كتابتها في `import_rows.raw_data.__relationship_hints`.
- [ ] زر "New Opportunity" بصفحة الحساب يُنشئ فرصة بلا RFQ مقابل — المواصفة (§25/§33) لا تعرّف فرصة قبل وجود RFQ. إمّا التقاط RFQ ضمن نفس التدفق، أو تحويل الزر لإنشاء BD lead. مترتّب على قرار D6 في `docs/DECISIONS.md`.
- [x] ~~`tender_bafo` غير قابلة للوصول~~ — أُصلحت 2026-08-06 (PR #178): خريطة الخادم صارت مطابقة للواجهة، مع contract test يقارن الخريطتين (`src/lib/tender-transitions.contract.test.ts`) فلا تتكرر عند إضافة مرحلة جديدة.
- [ ] مراجعة إعلان تبعيات Edge Functions: النشر يطبع `WARNING: Functions using fallback import map` — الـCLI يوصي بـ`deno.json` لكل دالة بدل الـimport map المشترك. رُصد أثناء نشر `sales-os-api` v41 (2026-08-05)؛ غير حاجب لكن الـfallback مرشَّح للإزالة.
- [ ] **لا تحذف حسابات `PW *`** — هي fixtures اختبارات Playwright المربوطة بـGitHub Secrets ووظيفة `playwright-smoke` (تعمل على كل PR). راجع `docs/playwright-test-setup.md`. سُجِّل هنا بعد أن كاد يُطلب حذفها بالخطأ (2026-08-05).
- [ ] `opportunity_flags` و`stage_transition_history` أعمدتهما polymorphic بلا FK — حذف فرصة يترك أيتامًا صامتة (وقع فعلًا 2026-08-05، 11 صفًا نُظِّفت يدويًا). الخيارات: trigger تنظيف، أو FK جزئي، أو فلترة الأيتام في استعلامات الواجهة.
- [x] ~~تأكيد ميداني لإصلاح PR #171~~ — **تأكَّد 2026-08-06**: `RFQ-2026-0005` (أُنشئ 05:52 على الإنتاج) مربوط بفرصة، تصنيفه `jih`، وفرصة "Janadriya" معه بـstakeholder واحد. أي أن إصلاحات #171 و#175 تعمل بيد مستخدم حقيقي.
- [ ] **فحص متصفح قبل شحن أي تغيير واجهة، لا بعده.** PRs #169–#172 شُحنت على typecheck + 672 اختبارًا + CI أخضر، وكلها عميت عن ثلاثة حقول فارغة على الشاشة (PR #175). اختبارات فحص المصدر لا ترى لوحة فارغة. يستحق النظر: فحص /qa قصير ضمن تدفق الشحن لتغييرات الواجهة.
- [x] ~~جدولة `run_automations` محجوبة بالمصادقة~~ — **حُلّت 2026-08-06** بإزالة المشكلة لا بالالتفاف عليها: القواعد نُقلت إلى دالة SQL (`public.run_sales_automations`، migration `20260806120000`)، وpg_cron يستدعيها داخل قاعدة البيانات مباشرة — بلا HTTP ولا بوابة ولا توكن ولا مفتاح خدمة مخزَّن (سرّ Vault حُذف). الدالة المُعرَّضة عبر Edge Function صارت تستدعي نفس دالة SQL، فالزر اليدوي والجدولة الليلية ينفذان نفس المنطق. تحقُّق حي على الإنتاج: التشغيل الأول رفع 4 بنود، والثاني والثالث صفر، وبعد إغلاق البنود يدويًا التشغيل التالي صفر أيضًا.
- [ ] **فصل المهام — قراران للعمل لا للكود** (`docs/security/separation-of-duties-2026-08-06.md`): (أ) حساب واحد يحمل **ثمانية أدوار** ويستطيع إتمام خطوات BAFO الأربع كلها بنفسه، فالضبط الرباعي صوري لذلك الحساب. (ب) **Ahmed Zaid بلا حساب** رغم أنه المدير المالي ومدير التقدير — الدوران على حساب آخر، فقرارات التقدير والمالية تُنسب لغير صاحبها في سجل التدقيق، و`rfqs.assigned_to` بلا شخص صحيح يُشار إليه. يحتاج بريده الوظيفي. (ج) لا يوجد منع للاعتماد الذاتي في سلسلة BAFO — **لا تُضِفه الآن**: بثلاثة مستخدمين حقيقيين سيُعطِّل العمل. الترتيب: صحّح توزيع الأدوار أولًا، ثم أضِف المنع.
- [ ] [[أضف أفكارك هنا]]
