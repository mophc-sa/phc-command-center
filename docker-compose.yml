name: phc-command-center

services:
  app:
    container_name: phc-command-center-app
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env.local
    volumes:
      - .:/app
      - /app/node_modules
    command: ["bun", "run", "dev", "--", "--host", "0.0.0.0"]
